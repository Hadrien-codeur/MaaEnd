# 苏白易连滑首轮日志分析

2026-09-14，源码 `7ede00ca`。博士反馈连滑过程正常，到末架后停留较久，随后人工下索，任务中断。

## 已确认的直接原因

执行器停在等待第 15 次 E 提示，尚未进入末架定位。14 次 E 均有独立成功动作和提示消失记录；之后等待 30 秒，因 `zipline_relay_prompt_timeout` 自行失败。

结合博士已到末架的实机反馈，本路线的正确计数应为：16 架 / 15 跳 = 首跳左键 + 14 次 E。现有预算按此前明确要求设置为左键后 15 次 E，因此多等了一次提示。没有证据表明这次是末架定位慢或按键漏计。

## 日志证据

原始日志留在本机，不提交。以下行号对应本次日志，后续滚动可能变化。

| 时间 | 事件 | 证据 |
| --- | --- | --- |
| 00:43:22.505 | 独立测试任务开始 | `install/debug/maafw.log:7130`，任务 200000001 |
| 00:43:44.386 | 首跳左键后进入连滑，E 预算 15 | `install/debug/cpp-algo/debug/maafw.log:1064` |
| 00:44:56.629 | 第 14 次 E 发出 | C++ 日志第 6235 行，pressed=14、required=15 |
| 00:44:56.919 | 第 14 次提示消失 | C++ 日志第 6256 行 |
| 00:45:27.028 | 等待下一提示超时，进入 Failed | C++ 日志第 8423～8425 行，awaiting_clear=false |
| 00:45:27.264 | 框架记录任务失败 | 主 maafw 日志第 53531～53532 行 |

主框架日志按 action_id 去重后得到 14 个成功的 `MapNavigatorZiplineRelayPress` 动作，C++ 同样记录 14 次 E 和 14 次提示消费。连滑开始后 `PositionProvider::Capture` 次数为 0，没有 input complete 或 endpoint confirmed 事件，因此定位逻辑根本没有启动。

日志不记录博士实体鼠标操作的准确时间，不能据此确定人工下索发生在自动超时之前还是之后。已确认的自动终止原因是提示超时。

## 修复方向

将本路线首跳之后的 E 预算改为 14；一般 N 架完整段为首跳左键加 N−2 次 E，4 架示例为 2 次 E。保留现有提示去重及末架定位确认，不通过延长超时掩盖计数差一。

博士已明确接受修复：“应该减2，按14次来”。现已将运行时预算、原生预览输出和验证工具统一为 N−2；保留完整 15 跳，未把路线裁成 14 跳。计数测试验证第 14 次提示消失后即可进入末架确认，两架单跳不发送 E。修复版尚待复测实际末架确认、自动下索及终点步行。
