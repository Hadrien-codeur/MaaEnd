# 新版固定滑索送货接力记录

> 更新日期：2026-09-10。阶段：P0 实机基线待完成；P1 已保存苏白易地图确认架序，尚未实现固定逐跳或连滑。
> 公司新版根目录：`E:\MaaEnd V2`。分支：`codex/fixed-zipline-delivery`。
> 每次开工先检查 Git 的实际状态，再读取本记录；每次收尾更新，不在接力后删除。

## 1. 已确认范围和授权

用户于 2026-09-07 确认 [开发计划](./新版固定滑索自动送货开发计划.md)，于 2026-09-08 确认启动新版独立仓库、采用 [项目规范](./新版固定滑索送货开发规范.md)，并通过个人 GitHub 仓库同步代码与接力文档。

从 P0 继续，随后 P1 补路线证据、P2 固定逐跳、P3 途中连滑、P4 武陵城双入口、P5 试验园区。使用上游 AutoDelivery 和 MapNavigator，不恢复 MapTracker。尚未完成 P0 游戏基线，不提前宣称功能可用。

## 2. 当前工作状态

- 已建立独立 Git 仓库，源码直接来自上游；没有共享旧仓库对象、安装和资源联接。
- 已创建 `codex/fixed-zipline-delivery`，`origin` 为上游只读来源，`myfork` 为个人仓库，默认向个人仓库推送；本机禁用 origin 推送并设置仅快进 pull。
- 已迁入完整已确认计划，保留原调研证据；新增本规范和两种 Agent 阅读入口。
- uv 0.12.10 已下载到 `.cache/tools/uv`；Python 依赖按 `uv.lock` 安装到 `.venv`，前端工具依赖按 `pnpm-lock.yaml` 安装到 `node_modules`。
- 三个子模块已全部检出主仓库指定提交；MaaDeps、WebView2 SDK、MaaFramework、MXU 和同基线的预编译 C++ Agent 已安装。
- Go Agent 已本地编译，资源和 Schema 检查通过；1279 项节点识别用例通过。送货生成器 46 项测试中 44 项通过、2 项为未修改的上游基线失败，详见下文。
- MapNavigator 启动检查通过：预编译 C++ Agent 连接成功，导航网格加载成功，查询得到 84 个区域。未连接真实游戏进行移动，检查后已停止临时服务及其 Agent。
- 微软 Build Tools 已安装到新版 `.cache/BuildTools`，MSVC/CMake/Ninja/Windows SDK 可用；本轮已完成本地 C++ 编译安装。首次构建前 MaaDeps 已有部分展开文件，但缺少 maadeps.cmake；已从本地 tarball 完整解包后编译通过。
- 本机 C++ 工具链和本地编译已核实；家里电脑仍需独立检查，不能把公司环境状态视作家里环境已就绪。
- 当前没有固定架序或连滑功能代码；已完成当前账号森空岛滑索坐标导入，`map01` 475 个设施标记/6 个 Level，其中滑索架 147 个；`map02` 651 个设施标记/9 个 Level，其中滑索架 210 个。原先将总计 1126 个设施标记称作滑索架不准确，其中包含供电桩、中继器等。尚未完成路线实测和游戏内送货测试。
- 博士已确认先做苏白易，并在地图预览中确认 15 架顺序，详见 [苏白易架序确认记录](./苏白易架序确认记录.md)。仅地图确认，下一次按 [P0 实机验收清单](./新版固定滑索送货P0实机验收清单.md)继续。

首次文档提交 `1b244876b5f45f5ad1f5ff66cfef03e1839d9895` 已推送并核实远端一致。此后的验证结果补充随本文件的收尾提交同步；最新提交以 Git 实际状态为准，避免在提交内容中循环填写自身 SHA。收尾时比较 `git rev-parse HEAD` 和 `git ls-remote myfork refs/heads/codex/fixed-zipline-delivery`。

## 3. 基线和上游变化

| 对象 | 版本或位置 |
| --- | --- |
| 当前开发基线 | `01ef330ad467f00fa6208f06a0eafdb408fbfa58` |
| MaaUtils | `6e9ba33f6ad835418097d9324c01c44a82825a2b` |
| MaaEnd-AI | `746e0854cad7977a9ec672b338c168a547b0b577` |
| MaaEndTestset | `c800a4ef7a93a420e883b9d9e4541936b2d94d2b` |
| 上次计划调研基线 | `cc3b79c9cbc156ee30cf9ea6e3dbb594551750db` |
| 个人旧固定滑索分支 | `feature/zipline-fast`，调研证据 `649ca85082acf5e85f95ddb4376793b0045f0d71` |
| 早期个人自送分支 | `feature/self-deliver-route`，证据 `b1fccddd67c479eaa292d0fa6e04c1fadd4b8038` |
| 上游 | <https://github.com/MaaEnd/MaaEnd> |
| 个人新版分支 | <https://github.com/Hadrien-codeur/MaaEnd/tree/codex/fixed-zipline-delivery> |

昨日基线之后共 7 个提交。对本项目主要影响：

- `38c15b5d`：虚拟禁区编辑与规划器避让。
- `efacac15`：运行期虚拟禁区用于绕障恢复。固定段失败策略需与该重规划行为衔接。
- `f4c44dae`：MaaUtils 更新以适配 MaaFramework v5.13.0，不能沿用昨日的依赖指针。
- `d66b675f`：3D 工具轨迹高度及观察者模式；取路线证据应使用当前工具。
- `01ef330a`：动作策略集中到 `ActionTraits`；`AbandonZipline` 改用 `IsContinuousRun()`。后续架序扩展需参考新结构。
- 另两项为 PlayCover 输入适配和赠礼修复。

已核对 `navigation.go` 仍从 `attach.zip` 读取配置，`ZiplineStore` 仍按地图存储，没有账号隔离字段。以上是源码核查，不是实机结论。相关代码与依赖更新必须在 P0 重新验证；不要自动纳入未合并分支。

## 4. 本机环境

| 工具 | 本轮状态 |
| --- | --- |
| Git | 2.53.0.windows.2 |
| Node | v24.14.1 |
| pnpm | 项目实际使用 10.28.0，遵循 packageManager；全局版本不作为项目版本 |
| Go | go1.26.2 windows/amd64 |
| Python | 3.13.13，本项目有独立 `.venv` |
| uv | 0.12.10，位于 `.cache/tools/uv` |
| Python MaaFramework | uv.lock 锁定 5.13.0b4；不等于安装目录 DLL 已验证 |
| 安装目录 MaaFramework | v5.13.0，已通过原生 Agent 连接与导航网格加载检查 |
| MXU | v2.5.2，程序 `install/mxu.exe` 已安装；未进行游戏任务验证 |
| MaaDeps | v2.12.6，按上游下载脚本安装 |
| WebView2 SDK | 1.0.2210.55，按上游脚本安装；2026-09-10 已由博士完成登录并导入设施 |
| Go Agent | 本机由当前工作分支构建，业务源码与 `01ef330a` 相同；初始化工具会再次编译 |
| C++ Agent | 2026-09-09 已由本机源码（业务基线 `01ef330a`）编译安装；早期使用官方构建 34144719594 |
| MSVC / CMake / Windows SDK | MSVC 19.44.35228、CMake 3.31.6 位于 `.cache/BuildTools`；Windows SDK 10.0.26100.0 位于系统 Windows Kits；本地编译已通过 |

当前终端环境设置：

```powershell
Set-Location 'E:\MaaEnd V2'
. .\tools\fixed-delivery\Enter-Dev.ps1
```

uv、Go 模块、Go 编译、npm 和临时目录都指向新版 `.cache`；pnpm 安装使用 `--store-dir .cache/pnpm-store`。这些设置不更改系统全局配置，换终端要重新执行。

安装来源和指纹：

| 文件 | SHA256 |
| --- | --- |
| `install/agent/go-service.exe` | `FDB94EC2B601522CE287BBE30832C66549685DF0F9D9411099B1E17BC097E1B9` |
| `install/agent/cpp-algo.exe` | `AC058D79A3B30126B3CC3A71DAA96A14EB2EE375B061AD3C588FCFD85212C8DE` |
| `install/maafw/MaaFramework.dll` | `D4503D525561A7BE46D7B2A5E64F3288F5B3BA7AAEFAF36AB44F7CE4C04CFFAE` |

`install/version.json` 的 `cpp_algo_sha256` 是下载 artifact 的摘要 `33d5b991916b36eef7ed1406c126de36c68ddf38d63dfccffd7298cd422cb91b`，不是上表 exe 文件的摘要。该版本文件仍保留下载信息，不代表随后本地编译的产物来源；以本轮编译记录和 exe 指纹为准。

已核实资源联接：`install/resource` 指向本仓库 `assets/resource`，`install/data` 指向 `assets/data`，`install/maafw` 指向 `deps/bin`。本版安装脚本将 `install/interface.json` 与 `assets/interface.json` 建为硬链接，已通过文件系统查询确认；这与上游部分文档的“复制文件”说明不同，应以每台机器的实际文件类型为准。

### 本轮验证结果

| 检查 | 实际结果 |
| --- | --- |
| 新增文档 markdownlint、Git 空白检查 | 通过 |
| 环境脚本 PowerShell 语法及执行 | 通过 |
| `pnpm check` | 通过，6 组资源配置，maa-tools 使用 MaaFramework 5.13.0 |
| Schema 校验，按上游 CI 指定目录范围 | 通过 |
| `pnpm test` | 通过，1279 项节点用例 |
| AutoDelivery / DeliveryJobs 生成器测试 | 44 通过、2 失败，共 46 项 |
| Go 本地构建 | 通过 |
| MapNavigator 启动、Agent 连接和区域查询 | 通过，load-status 为 ready，无错误，返回 84 个区域 |
| C++ 本地构建 | 通过，MSVC 19.44.35228、CMake 3.31.6，已安装 `install/agent/cpp-algo.exe` |
| 游戏内空跑和真实交付 | 未执行，已完成森空岛坐标导入，待按地图标记选择路线后实测 |

两项生成器失败均发生于未修改的上游文件；已用 `git diff 01ef330a` 核对送货生成器和 AutoDelivery 资源无差异：

1. `tools/pipeline-generate/DeliveryJobs/data.test.mjs:278`，物品选项数量断言在第 311 行得到 241，期望 242。后续只读对照确认：四号谷地和武陵的第一优先级选项均比模型数据少 `item_slwood_1_hshog_1_1`（香焗兽肝），没有多余选项。这是装箱选项完整性问题，不是已证实的滑索导航故障。
2. 同文件第 952 行的任务详情测试，在第 1186 行期望 `AutoDeliveryCheckDeliveryMissionSelected`，资源实际为 `AutoDeliveryInDeliveryMissionDetail`。后者同时检查 `InMenuMission` 和原有送货详情识别；表现为测试仍期待旧接线，不等同于已经证实游戏内卡死。此轮没有修改流程或测试断言。

当前仅记录原版基线差异，未修改断言使其强行通过。接入新版业务之前需复核生成源与用例，明确修复或更新上游的处理结果。

本机详细输出在 `.cache/check.log`、`.cache/schema-check.log`、`.cache/node-tests.log`、`.cache/delivery-generator-tests.log`、`.cache/setup-workspace.log` 和 `.cache/map-navigator-smoke.log`，这些日志不随 Git 同步。上面的结论、失败位置和重现命令已随文档保存。

## 5. 家里电脑首次接力

以下路径是示例，家里可替换成另一独立新版目录。目标目录需为空，不覆盖旧仓库。

```powershell
git clone --recurse-submodules --shallow-submodules --origin myfork --branch codex/fixed-zipline-delivery https://github.com/Hadrien-codeur/MaaEnd.git 'E:\MaaEnd V2'
Set-Location 'E:\MaaEnd V2'
git remote add origin https://github.com/MaaEnd/MaaEnd.git
git config remote.origin.pushurl DISABLED
git config remote.pushDefault myfork
git config push.default simple
git config pull.ff only
git status --short --branch
```

然后阅读本记录和规范，核对工具清单。GitHub 登录需要在该电脑独立完成，不复制另一台电脑的凭据。

若电脑已有 uv，可直接使用；否则在新仓库内下载本轮相同版本：

```powershell
New-Item -ItemType Directory -Force -Path '.cache/downloads','.cache/tools/uv' | Out-Null
gh release download 0.12.10 --repo astral-sh/uv --pattern uv-x86_64-pc-windows-msvc.zip --dir .cache/downloads
Expand-Archive -LiteralPath '.cache/downloads/uv-x86_64-pc-windows-msvc.zip' -DestinationPath '.cache/tools/uv'
. .\tools\fixed-delivery\Enter-Dev.ps1
uv sync --frozen
pnpm install --frozen-lockfile --store-dir .cache/pnpm-store
```

下载/解压命令仅用于首次安装；已有对应文件时先验证版本，不重复覆盖。随后按本记录实际完成状态补建环境：

```powershell
uv run --frozen setup-workspace --cpp-algo-run 34144719594
uv run --frozen build-and-install
uv run --frozen build-and-install --cpp-algo
```

`setup-workspace` 会下载依赖与预编译程序，可能选取 CI/Release；必须记录实际来源，不能自动认定与源码同一提交。涉及 C++ 修改后必须本地重新编译。资源目录联接只能指向当前新版仓库。

上面的 CI run 仅适用于本轮 `01ef330a` 业务源码基线；C++ 有改动后不得继续用该旧构建。CI artifact 也可能过期，需要本地编译或从已核实的同提交产物恢复。MaaFramework/MXU 下载脚本使用当时发布版本，家里安装后应与本记录对照，不能假定日期不同仍获得相同版本。

## 6. 日常接力与收尾

开工流程：先查看本地状态；有未提交改动时保留并辨明来源，再决定如何同步。下面 pull 和子模块更新只在工作区干净、子模块没有自有改动时执行：

```powershell
git status --short --branch
git fetch myfork
git log --oneline --left-right HEAD...myfork/codex/fixed-zipline-delivery
git pull --ff-only myfork codex/fixed-zipline-delivery
git submodule update --init --recursive
. .\tools\fixed-delivery\Enter-Dev.ps1
```

若分叉，先保留两侧提交并审查差异，不用强制覆盖。按变更更新依赖和重建 Agent，报告当前阶段、下一步后继续；总体方案已经确认，不反复要求重批。更新上游需要单独评估，不与日常同步混做。

收尾流程：更新本记录中的状态、验证、待办和环境，暂存本项目相关文件，以 `docs:` / `feat:` / `fix:` 等前缀提交，然后执行：

```powershell
git push myfork codex/fixed-zipline-delivery
git rev-parse HEAD
git ls-remote myfork refs/heads/codex/fixed-zipline-delivery
git status --short --branch
```

本地与远端提交号一致才报告已同步。安装包、日志和缓存不会随 Git 同步；游戏设施需要在新电脑按当前账号重新导入并验证。不要依赖旧聊天记录、临时目录或旧公司仓库才能继续。

## 7. 下一步与实机材料

1. 独立测试环境和自动检查已经完成；先核对本次同步结果及上述两项生成器基线失败，不重复下载已经验证的本机环境。
2. ~~安装并验证微软 C++ 工具链。~~ 已完成：工具链位于新版 `.cache/BuildTools`，本地 `cpp-algo` 编译通过。
3. 公司森空岛导入和苏白易地图架序确认已完成；家里在新版主程序重新导入当前账号设施，按已保存的坐标表匹配。
4. 先完成武陵城取货路线的上游步行、允许滑索两组空跑，记录耗时、落点、失败与货物状态，再做完整业务基线。
5. P1 补齐取货单跳、一段短链及各中间架坐标和 E 提示证据，之后才能进入 P2/P3。

## 8. 新会话启动语

> 继续新版固定滑索送货开发。我现在在公司/家里电脑，当前打开的是新版独立仓库。先读 AGENTS.md 和新版固定滑索送货接力记录，检查本地改动并同步 GitHub 上 codex/fixed-zipline-delivery 分支，核对依赖和程序版本，然后从记录的下一步继续。

## 9. 2026-09-08 首次公司到家里交接（历史）

公司本轮完成环境准备、规范和计划同步，阶段停在 P0，尚未实现固定架序和连滑。用户确认武陵城布局未变，实机验证另择时间。

家里首次开始时：

1. 新建独立空目录，例如 `D:\Github project\MaaEnd V2`，以此作为新会话的项目根目录；不要打开旧 MaaEnd 目录继续旧分支。
2. 从个人仓库 `https://github.com/Hadrien-codeur/MaaEnd.git` 获取 `codex/fixed-zipline-delivery` 分支，按本记录第 5 节配置。该分支已经含上游基线及本次工作，不必先克隆上游主分支再覆盖。
3. 检查 GitHub 登录状态；家里若已登录同一账号可复用，否则在家里单独登录，不传输公司凭据。
4. 拉取后先读本记录、规范和已确认计划，再核对家里依赖、模型和测试程序。公司 `.cache`、`.venv`、`install` 和编译器不会随 Git 同步，不需要搬运整个公司目录。
5. 优先完成家里 C++ 工具链与本地编译，再按实机清单导入设施并验证上游路线。公司 C++ 下载是否结束不影响家里独立准备。
6. 家里收尾同样更新记录并推送本分支。下次回公司先检查本地状态、拉取家里的更新，再按变更重建程序，不能直接运行下班前的旧测试包。

本次交接提交只更新文档；用户自行启动的下载或安装进程未被本次交接操作暂停、取消或代为判定成功。

## 10. 2026-09-10 离开公司接力保存点

博士昨天回家未开发，本轮仍在公司新版目录；现在准备回家继续。已保存 [苏白易架序确认记录](./苏白易架序确认记录.md) 和 [地图预览 JSON](./苏白易架序.preview.json)，包含最终 15 架/14 跳及各架世界高度。记录中的 #0～#14 与编辑器一致；此前编号修正过程作废，以已确认表格为准。

本轮保存前逐一将 15 个点匹配到公司导入快照：地图、Level、模板和投影坐标均唯一匹配。此检查不代表供电、实际连线、上索或 E 接力已通过。地图工具已启动并显示导入架子；博士截图显示游戏连接未建立，仍无实机空跑结果。此前仅运行 --help 的检查不能称作 Agent 运行验证；2026-09-10 真正启动工具时已看到 AgentClient connected 和 Tasker initialized。

家里继续顺序：

1. 在独立新版仓库切到 codex/fixed-zipline-delivery，检查未提交改动，fetch 后仅快进同步。首次搭建按第 5 节；旧版仅供只读参考。
2. 先读本记录、开发规范、确认计划以及苏白易架序确认记录。称呼博士，避免重复要求提供已确认的坐标。
3. 独立核对 C++/Go/依赖并构建；公司二进制、.cache、.venv、node_modules 不随 Git 同步。MaaDeps 除目录存在外必须检查 maadeps.cmake、头文件与库完整。
4. 从 install/mxu.exe 的滑索导入任务登录森空岛，导入同一账号设施。原始快照和凭据保留在本机，不进入 Git。
5. 打开 MapNavigator，按架序记录恢复预览。预览 JSON 只有普通 NAVMESH 标记点，不具备固定架序执行能力，不能作为固定滑索试跑入口。
6. 继续 P0 实机基线、P1 逐跳与地面段证据，然后推进固定逐跳实现。连滑分段和 E 提示、失败策略尚未确认。

本次只保存文档和预览资料，无业务实现改动；资源全量检查沿用第 4 节历史结果，不声称本轮重新执行。此前尝试 pytest 失败是因为环境没有安装 pytest；MapNavigator 现有测试使用 unittest，后续按实际测试入口运行，不为此盲目加依赖。

完整设施快照、原始截图、账号配置、日志和程序不提交。家里不依赖公司浏览器、剪贴板或 localhost:8770 服务，重新启动本机工具即可。收尾检查文档格式、预览 JSON、Git 空白和远端 SHA 后完成同步。
