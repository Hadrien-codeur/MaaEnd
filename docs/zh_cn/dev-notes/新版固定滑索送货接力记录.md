# 新版固定滑索送货接力记录

> 更新日期：2026-09-23（候选 relay 已改为确认起滑后接管，等待博士复测林德）。阶段：当前可用主分支远端保存点为 `a8c21918`；基于上游 `c9204400` 的候选适配位于独立分支 `codex/fixed-delivery-upstream-adapter`，最新提交 `81254d00`，已重建 staging 并重启 MXU。候选尚未合并、替换正式安装或通过实机验收；武陵城四个、试验园区三个送货终点的现行版本双入口实测结论保持不变。
> 公司新版根目录：`E:\MaaEnd V2`。分支：`codex/fixed-zipline-delivery`。
> 每次开工先检查 Git 的实际状态，再读取本记录；每次收尾更新，不在接力后删除。

## 1. 已确认范围和授权

用户于 2026-09-07 确认 [开发计划](./新版固定滑索自动送货开发计划.md)，于 2026-09-08 确认启动新版独立仓库、采用 [项目规范](./新版固定滑索送货开发规范.md)，并通过个人 GitHub 仓库同步代码与接力文档。

从 P0 继续，随后 P1 补路线证据、P2 固定逐跳、P3 途中连滑、P4 武陵城双入口、P5 试验园区。使用上游 AutoDelivery 和 MapNavigator，不恢复 MapTracker。尚未完成 P0 游戏基线，不提前宣称功能可用。

## 2. 当前工作状态

### 2026-09-21 上游兼容迁移暂停保存点

- 当前可用分支 `codex/fixed-zipline-delivery` 停在 `c08802cd`，已同步 `myfork`；工作区除既有未跟踪 `outputs/` 外无改动。该版本继续作为七条路线已实测通过的回滚基线，未被候选迁移覆盖。
- 上游最新核对为 `origin/v2` 的 `c9204400`。独立分支 `codex/fixed-delivery-upstream-adapter` 已在该提交上保存适配成果，提交为 `15515945`；本机 worktree 为 `.cache/fixed-delivery-migration-c920`。此提交是待完成、待实机回归的开发保存点，不得直接合并主分支或替换正式安装。
- 已迁移 AutoDelivery／DeliveryJobs 生成器、七条固定路线目录与任务选项、五语言文案、Go 分发、固定路线 Schema，以及 MapNavigator 的固定架序、设施唯一匹配、固定段失败不降级、专用路径展开和连滑接力框架。AutoDelivery 与 DeliveryJobs 资源生成成功，七个终点均生成 `fixed_route_node`。
- 本轮验证：主分支兼容检查器检查迁移 worktree 通过；`go test ./autodelivery` 通过；改动 Go 文件 `gofmt` 检查通过；相关 JSON／MJS 的 Prettier 检查通过；最新上游 C++ Agent 已使用 MSVC 和 `-DCMAKE_CXX_FLAGS=/EHsc` 编译成功，候选程序位于迁移 worktree 的 `.cache/cpp-build-fixed/bin/cpp-algo.exe`。未运行迁移分支全量 `pnpm check`、`pnpm test` 和 Schema／C++ 局部测试，不扩大解释为全量验证通过。
- 未完成项：`dismount_heading` 尚未完整接入最新 `ZiplineRideMachine` 的下索前朝向；固定离索路径尚未从实测下索定位重新接入首点；连滑分段边界和 E 预算尚缺局部测试及实机证明。上游候选版本的七条路线因此仍标记为“未实机验收”。
- 明日继续时先完成上述三项 C++ 适配，再补固定架序、缺架／歧义／错地图、固定段失败不降级、relay E 预算和分段边界测试；随后运行完整生成、Schema、`pnpm check`、`pnpm test`、Go 测试和 C++ 构建。全部通过后仍先保留在候选分支，由博士执行迁移版实机回归，成功后才允许原子切换正式安装。

### 2026-09-22 上游候选适配完成

- 候选分支已推进至 `02e1c7e7` 并推送 `myfork/codex/fixed-delivery-upstream-adapter`。固定路线链尾已接入新版 `ZiplineRideMachine` 的下索前朝向闭环；末架落地后先取得稳定实测定位，再从该位置重新展开固定离索段，无法接入时停止而不跟随名义落点；连滑边界增加无符号范围保护，并保留 0／1／2 跳 E 预算编译期断言。
- `MapNavmeshQuery` 补充 `relay_presses_after_launch` 与 `dismount_heading` 只读诊断字段，便于新版候选离线验收。候选 staging 位于迁移 worktree 的 `install`，依赖和当前账号 `Ziplines.json` 仅用于本机验证，未进入 Git。
- 为便于实机回归，候选 staging 已补入 `mxu.exe` 与 `version.json`；没有复制稳定目录的 MXU 实例配置或账号配置，首次启动应在 staging 目录生成自己的配置。
- 候选验证：兼容门禁通过；AutoDelivery／DeliveryJobs 串行生成成功；Prettier、`pnpm check`、`pnpm test`、`go test ./autodelivery` 通过；C++ Agent 使用 MSVC 构建安装成功；七条固定路线离线预览通过，武陵城／试验园区跳数为 15／10／8／11／3／5／5，E 预算为 14／9／4／8／2／4／4，下索朝向 294／101／108／323／343 均正确透传。
- staging 失败矩阵也已通过：未知路线返回 `fixed_zipline_unavailable` 且不回退；关闭 `zip` 在参数解析层拒绝；错地图层返回 `fixed_zipline_wrong_zone`；插入必经语义点返回 `fixed_zipline_unsupported_path`。
- 当前仍未完成：博士使用候选 staging 在游戏内回归七个终点，以及固定开关关闭、全局禁滑、未配置终点、已有持货恢复和固定段失败停止矩阵。完成这些实机证据前，不替换 `install`，不把候选版标为正式可用。

### 2026-09-22 今日收尾：候选 staging 暂停，等待实机回归

- 已核对候选 staging 的 `debug/2026-09-22-1.log`、`debug/maafw.log` 和 Agent 调试日志。02:23 前后只有 MXU 资源加载、配置初始化和定时器扫描；未出现游戏连接、`SeizeDeliveryJobs`／`DeliveryJobs` 启动、送货节点、固定路线执行或任务结果。因此本轮不新增任何实机通过结论，也不把候选 staging 的启动视为验收。
- 已关闭候选 staging 的 MXU 进程；候选 `cpp-algo`、`go-service` 在收尾检查时已不在运行。稳定安装目录未替换，候选安装和账号／设施快照仍只保留在 `.cache/fixed-delivery-migration-c920/install`，不进入 Git。
- 七条路线的 relay 分段诊断记录统一为：苏白易 `{0: 14}`、林德 `{0: 9}`、玉石 `{0: 4, 5: 2}`、回收站 `{0: 8, 9: 1}`、赵昭 `{0: 2}`、裴令容 `{0: 4}`、阿禾 `{0: 4}`。其中玉石和回收站的第二段分别从第 5 架和第 9 架重新瞄准；这只是离线／诊断字段，不替代游戏内 E 提示证据。
- 今天的开发保存点为稳定分支 `df48671f` 与候选分支 `02e1c7e7`；下一次直接从候选分支继续，不重做 2026-09-21 已完成的迁移工作，也不先覆盖稳定安装。
- 本次文档收尾产生本地提交 `92a171db`；由于当前电脑直连 GitHub 的 443 端口超时且没有可用本机代理，`myfork/codex/fixed-zipline-delivery` 暂仍停在 `df48671f`，不能报告为已推送。下次开工先恢复网络并 push 后再接力。

### 2026-09-22 晚间接力核对：远端同步恢复，下一步仍为候选实机回归

- 已重新 fetch `origin` 与 `myfork`。`origin/v2` 仍为 `c9204400`，没有出现需要重新迁移的上游提交；候选分支本地与 `myfork/codex/fixed-delivery-upstream-adapter` 均为 `02e1c7e7`。
- 上次因网络失败滞留的两个接力文档提交已推送；稳定分支本地与 `myfork/codex/fixed-zipline-delivery` 现均为 `228dfa3a`。未跟踪的 `outputs/` 表格没有进入提交或推送。
- 本轮新增的状态核对文档已在本地提交，但随后 GitHub 443 连接再次失败，尚未推送；远端仍停在 `228dfa3a`。下次接力先比较本地与远端并补推，不要覆盖本地提交。
- 候选 staging 仍位于 `.cache/fixed-delivery-migration-c920/install`，程序、资源和配置均在。博士确认可以关闭原有实例后，已终止 22:28 启动的旧 `mxu.exe`，并从候选 staging 启动新窗口；候选 PID 为 `19032`，窗口标题为 `MaaEnd v0.1.0`，已生成 `debug/2026-09-22-2.log`。尚未连接游戏或启动任务，因此仍不新增实机通过结论。
- 当前不需要继续改代码或重复离线检查。下一步先用候选 staging 各完成一个武陵城、试验园区最小烟测，再按七终点双入口和异常矩阵收集实机证据；全部通过前不合并候选分支、不替换稳定安装。

### 2026-09-22 候选武陵城烟测失败：连滑在起滑前误入接力轮询

- 博士使用候选 staging 运行武陵城抢委托。23:44 与 23:46 两次取货固定路线因当时设施快照未更新而在动作前返回 `fixed tower #0 missing or unpowered`；23:47 重新导入 `Ziplines.json` 后，第三次 `wuling_city_pickup` 唯一匹配两架、完成上索／发射／落地／下索，并于 23:49:07 返回成功。该前置问题已由重新导入解决，不是本次最终失败根因。
- 正式终点解析正确命中林德 `deliver_target_map02_lv002_02` 和 `wuling_city_lind`。11 架固定顺序唯一匹配，路径按指定顺序展开为 42 个运行点，没有发生缺架、歧义、错地图或自动路线降级。
- 23:49:34 上索提示动作刚完成时，`ZiplineRideMachine` 仍处于 `mounting`；错误截图中角色仍站在首架前，黄色“乘上”提示尚在。候选代码已经把 10 跳／9 次 E 的 relay 状态置为活动，并在调用 `ride.Tick()` 完成上架、瞄准和左键起滑之前优先进入 E 提示轮询。
- 首帧 `RealTimeAutoZipline` 得分约 `0.389`，正常未命中；Pipeline 随即通过 `MapNavigatorZiplineRelayEnd` 的 `DirectHit` 正常结束。但迁移版 `RunNodeAndReportHit(..., &succeeded)` 只在目标识别节点本身出现在节点明细中时设置 `succeeded=true`，把“子任务成功、提示未出现”误报为失败，最终返回 `zipline_relay_pipeline_failed`，严格停止固定路线。
- 修复需同时处理两个边界：先让原 `ZiplineRideMachine` 推进到已经实际发射／滑行的阶段，再接管 E 提示；并把“子任务执行成功”与“本帧提示命中”拆成两个独立结果。不能通过 `pre_delay` 等待提示，也不能只改布尔返回而让 `mounting` 状态机继续饿死。补测试至少覆盖起滑前多帧无 E、起滑后提示出现／消失去重、真实 Pipeline 动作失败三类情况。当前候选二进制无需重复实测，修复、局部测试、重编译并替换 staging 后再跑林德烟测。

### 2026-09-23 候选连滑起滑时序修复完成，等待林德复测

- 候选分支修复提交 `c25952b5` 已推送至 `myfork/codex/fixed-delivery-upstream-adapter`。`RunNodeAndReportHit` 改为分别返回“子任务成功”和“目标节点命中”，正常无提示帧不再触发 `zipline_relay_pipeline_failed`；上索、架上确认、俯仰重置等既有调用仍只依据目标节点是否实际完成。
- 新增 `ZiplineRelayShouldPoll` 阶段门禁：`Mounting`、`OnTower`、`Aiming` 均不允许 E 轮询，原 `ZiplineRideMachine` 先完成上架、瞄准和左键发射；进入 `Fired` 或 `Riding` 且 E 预算未完成后才接管提示。0 次预算、已完成预算和落地阶段均不轮询，相关边界由 C++20 `static_assert` 随生产目标编译验证。
- 没有修改 Pipeline、路线数据或识别阈值，也没有加入硬延迟。MSVC RelWithDebInfo 构建与安装成功，`pnpm check`、`pnpm test` 通过；首次直接复用旧 Ninja 目录因当前 PowerShell 未加载 MSVC 标准库环境而失败，随后由正式 `build_and_install` 脚本加载 VS 环境成功重建，不属于源码失败。
- staging 新 `cpp-algo.exe` 大小为 3,219,456 字节，SHA256 为 `6DFFF6B245221E6D32DE5266E4A5C0D02070091B8854EE835FC3A99CF69FC414`。候选 MXU 已于 00:09 重启，PID `18756`，窗口正常响应，并生成 `debug/2026-09-23-1.log`。
- 下一步由博士重新运行武陵城抢委托林德路线。先确认日志依次出现 `mounting`、`on_tower`、`aiming`、`fired`，再确认 9 次 E 提示消费和末架落地；该单点通过后再继续另一个地区烟测与完整七终点矩阵。

### 2026-09-23 林德两次复测未起滑：`Fired` 尚未证明发射成功

- 博士连续两次复测林德路线，分别于 00:16:23、00:18:15 手动停止。两次均正确解析 `deliver_target_map02_lv002_02`，唯一匹配 11 架固定路线并展开 42 个运行点；上索 OCR 均识别到完整“登上滑索架”，第一次 Alt 点击实际命中识别框 `[871,462,87,23]` 内的 `[924,472]`，随后两次都出现 `zipline/mount/confirmed`。
- 两次状态序列均为 `mounting -> on_tower -> aiming -> fired`，分别在 00:15:22、00:17:26 进入 `fired`；之后没有任何 `zipline/fired/riding`、`zipline/fired/no_launch`、`ZIPLINE relay E pressed` 或提示消费记录。当前 `c25952b5` 将 `Fired` 也判为可轮询 relay，因而从下一拍开始持续跑 E 模板，原 `ZiplineRideMachine::TickFired` 再也没有执行机会，既无法确认起滑，也无法识别空响后调整俯仰重发。
- 已实测旧版在 `StartZiplineHop` 内同步完成上架确认、瞄准和 `FireLaunch`，随后才建立 relay；上游 `c9204400` 则把发射确认拆进 `Fired -> Riding/Landed/no_launch` 状态机。候选把旧版接管位置直接移植到新版分阶段状态机后形成了所有权冲突。上次修复已恢复“无提示帧是正常结果”，但把 `Fired` 当成“已成功发射”仍然过早。
- 正确修复应让 `Fired` 继续由 `ride.Tick()` 驱动，仅当状态机通过连续定位丢失确认进入 `Riding` 后，relay 才停止中间定位并接管 E 提示。这样空响仍能走上游的 `no_launch` 分类、俯仰档重试和失败出口，不需要硬延迟。relay 接管后还应恢复旧版按“E 按下／提示消失”重置的进度超时，提示长期不出现时明确报 `zipline_relay_prompt_timeout`，而不是无限轮询。
- 下一轮测试应补 `Mounting/OnTower/Aiming/Fired` 不接管、`Riding` 才接管、`Fired` 空响仍调用原状态机、无提示超时四类覆盖；然后重新构建 staging，再跑林德单点。当前 `c25952b5` 不继续重复实测。

### 2026-09-23 确认起滑后接管 relay 的修复已安装

- 候选修复提交 `81254d00` 已推送至 `myfork/codex/fixed-delivery-upstream-adapter`。`ZiplineRelayShouldPoll` 现仅允许 `Riding`，`Fired` 会继续交给上游 `ZiplineRideMachine::TickFired`，因此左键空响仍能进入 `no_launch` 分类、调整俯仰并重发，确认定位连续丢失、实际滑出后才停止中间定位并接管 E 提示。
- relay 在首次进入 `Riding` 时建立进度时钟，E 按下或已消费提示消失时刷新；超过既有 `kZiplineRideTimeoutMs` 没有新进度则返回 `zipline_relay_prompt_timeout`。E 预算未完成却提前得到落地事件时返回 `zipline_relay_landed_early`，不再把第一跳误记成整段到达。
- C++20 编译期断言覆盖 `Mounting/OnTower/Aiming/Fired/Landed` 不轮询、`Riding` 待完成预算才轮询，以及超时边界。MSVC RelWithDebInfo 构建安装、clang-format 检查、`pnpm check`、`pnpm test` 均通过；第一次构建因重复前置声明导致重载歧义，删除多余声明后完整构建通过，最终源码和安装产物不含该编译错误。
- staging 新 `cpp-algo.exe` 大小为 3,220,992 字节，SHA256 为 `ADBEA2C6F04313C52185C6A0AC3EC5AE97EDF7CDF5AF9912B57B3FEB370F4BD0`。MXU 已于 00:43 重启，PID `20452`，窗口正常响应；下一步复测林德，先看 `fired -> riding -> relay monitoring started`，再核对 9 次 E 与末架落地。

### 2026-09-20 七条路线正式双入口通过

- 博士反馈已使用抢委托送货与转交委托，对武陵城四个、试验园区三个送货终点逐项完成实际固定滑索送货，七条路线均通过。正式业务验收矩阵已登记为“已通过（反馈）”，逐笔原始日志和截图待归档，不据此虚构未提供的时间、提示或落点细节。
- 该结果满足固定路线正式接入的成功订单门槛，下一步从“继续扩路线”转为“冻结路线接口并迁移上游版本”。持货恢复、固定开关/全局禁滑分流、未配置终点和失败停止仍是独立异常矩阵，不因成功订单自动通过。
- 版本迁移必须先检出最新上游 `v2`，再通过固定滑索适配层重建生成资源和 Agent；不能直接把当前分支的 MapNavigator 提交整体套到上游。兼容检查与候选安装通过前，保留当前版本作为可回滚基线。

### 2026-09-20 正式订单实测等待点

- 本机分支与 `myfork/codex/fixed-zipline-delivery` 已同步到 `cb4975ed`，包含控制器限制、回归修正和正式业务验收矩阵；安装目录日志最后更新时间为 2026-09-19 23:54，未发现新的正式抢单或装箱自送订单证据。
- 试验园区三条固定路线已纳入原生离线规划覆盖，共 14 项测试通过；新增正式入口回归测试后，AutoDelivery 与 DeliveryJobs 生成器测试合计 56/56，通过项明确验证试验园区在抢单来源、装箱自送区域、两级固定滑索选项及三个 `fixed_route_node` 上完整接线。上述覆盖验证架序、E 预算、下索朝向、录制离索段与严格终点，不替代游戏内交付提示和任务退出验收。
- 固定滑索选项现仅对 `Win32-Front` 开放；普通送货任务仍保留其他控制器。旧的 DeliveryJobs 任务详情断言已按当前生成 Pipeline 更新，资源检查、Schema 校验和 1279 项节点测试均通过。
- 下一次实机应使用正式 `SeizeDeliveryJobs` 与 `DeliveryJobs` 入口，记录七个终点的 `source_id`、实际固定节点、交付提示、任务退出和持货恢复；同时至少验证固定开关关闭、全局禁滑、未配置终点与固定段失败停止。
- 当前逐项记录模板见 [新版固定滑索正式业务验收](./新版固定滑索正式业务验收.md)；在七个终点和两类入口获得订单日志前，不把共享接线标为正式业务通过。

### 2026-09-19 正式入口接线前测试收口

- 核对确认正式 `SeizeDeliveryJobs` 与 `DeliveryJobs` 均通过共享 `AutoDelivery` 按终点 `source_id` 选择固定路线；武陵城四个终点和试验园区三个终点均已有 `fixed_route_node`。不新增业务 Pipeline，不改变试验园区仓储的 `walk_only: true`。
- 发现并修正 Go 分发测试遗漏：`agent/go-service/autodelivery/navigation_test.go` 原只列武陵城四个固定终点，已补入 `deliver_target_map02_lv005_01`、`..._02`、`..._03`。
- 生成器测试 56/56、正式任务选项测试 1/1、AutoDelivery Go 分发包测试、Schema 校验、`pnpm check` 和 `pnpm test`（1279 项）均通过。全仓 `go test ./...` 仍受既存 `agent/go-service/ims` 测试缺少 `simulateRestart` 影响，本轮未扩大修复范围。用户反馈的两地区路线通过属于独立路线空跑证据，正式订单的交付提示、任务退出、持货恢复和异常停止仍待实测。
- 新增并通过试验园区三条 `WithFixedZipline` 原生离线规划覆盖：共 14 项测试，验证赵昭／裴令容／阿禾的架序、4/4/2 次 E、下索朝向、录制离索折点和严格终点；固定路线工具 6/6、接力 Pipeline 2/2 也通过。离线测试不替代真实订单验收。
- 代码和文档收口提交 `56123169`、测试覆盖补强提交 `2b9777d8` 已推送到 `myfork/codex/fixed-zipline-delivery`；继续用正式抢单和装箱自送入口覆盖七个终点及默认关闭、全局禁滑、未配置终点、已有持货恢复和失败停止场景。

### 2026-09-19 同步接入转交委托入口

- 博士要求将试验园区固定送货同步接入转交委托。检查确认 `DeliveryJobs` 顶层已使用共享 `DeliveryJobsAutoDeliveryPreferZipline`，其 `Yes` 分支继续展开 `DeliveryJobsAutoDeliveryFixedZipline`；固定开关会通过共享 AutoDelivery 根据终点目录自动选择试验园区三个 `fixed_route_node`，无需复制转交流程。
- 五语言 `task.AutoDeliveryFixedZipline.description` 已补充武陵城与试验园区范围；默认关闭、全局禁滑分流和未配置终点自动规划语义不变。`DeliveryJobs` 的选项链和生成资源未改动。
- 本轮只完成入口说明同步，尚未执行装箱自送真实订单。下一步在转交委托中选择试验园区仓储，开启送货时优先使用滑索及固定滑索路线，先验证已有委托/新装箱两条路径的固定送货和失败出口。

### 2026-09-19 扩展固定滑索接单实测入口至试验园区

- 博士要求先复用 `FixedWulingSubaiyiDeliveryTest` 做试验园区真实接单实测。检查确认该任务沿用 `SeizeDeliveryJobsMain` 与正式三组选项，来源选项已经包含 `WulingCity`、`TestArea`、`Unlimited`；完整送货下的“送货时优先使用滑索”会继续展开“使用已配置的固定滑索路线”，无需复制业务 Pipeline 或新增任务 ID。
- 五语言任务名称和说明已扩展为“武陵城/试验园区”范围，明确固定路线默认关闭及开启顺序；内部任务 ID、入口和默认选项保持不变，避免影响既有 MXU 配置。
- `pnpm check` 与目标 JSON 格式检查通过。本轮只完成入口文案与范围说明接线，尚未执行真实订单；下一步博士选择 `TestArea` 来源、完整送货、送货时优先使用滑索、固定滑索路线后测试三个终点，并记录取货、固定送货和任务退出结果。

### 2026-09-19 试验园区三个送货终点独立空跑通过

- 博士反馈修复版三个试验园区空跑任务均没有问题，确认裴令容、阿禾、赵昭三条固定滑索及录制地面段可以在当前账号设施下完成独立空跑。本轮按博士反馈登记实机结果，不额外推断具体耗时、逐次提示日志或真实交付提示。
- 先前 `fixed_zipline_unsupported_path` 与 B/C 列离索段对调问题已由 `008f1e16` 修复；当前保留裴令容／阿禾／赵昭 4／4／2 次 E、各自下索朝向及最终站位，不再安排同一问题的重复空跑。
- 下一步进入 P5 正式业务接线：三个终点通过现有 `fixed_route_node` 接入共享抢单和装箱自送分发，继续沿用默认关闭的固定路线开关、未配置终点自动规划和全局禁滑步行分流。试验园区仓储取货仍保留 `walk_only: true`，不将本次送货空跑扩展为固定取货通过。
- 接线后依次验证真实抢单、装箱自送、已有持货恢复、成功退出与失败停止；在这些证据完成前，不将 P5 双入口或完整恢复矩阵记为通过。

### 2026-09-19 试验园区空跑失败排查与接线修复

- 博士反馈三个试验园区空跑任务均失败。本机 `install/debug/cpp-algo/debug/maafw.log` 核对到三次任务都能完成 DirectHit 和初始定位，位置约为 `[1252.88,1751.5]`，随后 `MapNavigateAction` 立即返回 `ret:false`；没有发生上索、E 接力或下索，因此不是实机接力失败。
- 离线 `route_preview` 复现第一根原因：试验园区仓储源的 `departure_path` 含 `required: true`，生成器把它拼入三个固定节点的 `path`；固定路线执行器明确拒绝带必经移动点的固定路径，返回 `fixed_zipline_unsupported_path`。已让 `WithFixedZipline` 节点复制路径坐标但去掉普通规划专用的 `required` 标记，普通步行和普通 `WithZipline` 节点不变。
- 离线复现还发现表格 B/C 两列的末段地面路径曾在源配置中对调：裴令容应接 `[1088.62,1462.1]` → `[1084.06,1455.88]`，阿禾应接 `[1370.25,1535.55]` → `[1369.66,1534.32]`，已按表格修正；赵昭段保持不变。
- 修复后离线固定路线预览全部通过：裴令容 5 段、阿禾 5 段、赵昭 3 段，分别到达 `[1084.06,1455.88]`、`[1369.66,1534.32]`、`[1383.91,1633.4]`。AutoDelivery 生成器 26 项、`pnpm check`、`pnpm test` 1279 项和格式检查通过；尚未重新执行游戏输入。
- 下一步博士重新运行三个 `FixedTestArea*Test` 空跑入口。若仍失败，再按日志区分“设施供电/实时连线”与“上索输入/接力/离索步行”；在修复版实机确认前，不接入正式抢单和装箱自送入口。

### 2026-09-19 试验园区路线接入源配置，等待独立空跑

- 博士将试验园区路线录入飞书表格“武陵城固定滑索路线填写表”的“路线填写（试验园区）”页。本机以用户身份读取 revision 177 的 A1:T200，实际内容到 A2:E24；账号与设施填写为“同一账号且设施未变”“沿用当前快照”，地图层级为 `Wuling_Base`。表格提供三个送货终点的完整架序、公共首架前路径、末架下索后的录制步行段和站位；“手动无订单跑通结果”“交付提示与截图”仍为空。
- 依据当前 `install/debug/record/Ziplines.json`，新增 `test_area_pei`（裴令容，6 架，1-6 连滑，4 次 E，323°）、`test_area_ahe`（阿禾，6 架，1-6 连滑，4 次 E，343°）和 `test_area_zhaozhao`（赵昭，4 架，1-4 连滑，2 次 E，108°）。`validate_fixed_routes.py` 已对三条路线逐架唯一匹配 `map02_lv005` / `0f45150a59b97bd0de9a4eed7a0fbf23`，供电和实时连线仍未由该脚本证明。
- 已修改 `fixed_zipline_routes.json` 与 AutoDelivery `routes.json` 源配置并重新生成 `Routes/TestArea.json`、`catalog.json`。普通步行、普通自动滑索、站位修正和仓储原有 `walk_only: true` 均保留；新增 `FixedTestAreaDestinationsTest`，提供三个 Win32-Front 独立空跑入口，五语言文案和 `interface.json` 已同步。
- 本轮自动验证：AutoDelivery 生成器 25 项、固定路线快照校验、原生固定路线测试 13 项、送货选项测试、`pnpm check`、`pnpm test` 1279 项、Prettier 检查和 Schema 校验均通过。系统 Python 缺少 `jsonschema`，因此未能运行 `test_fixed_route_tools.py`；`test_relay_pipeline.py` 仍因缺少 Pillow 未运行。未编译 Agent，未执行游戏输入或独立空跑，不把路线接线记为实机通过。
- 下一步由博士依次运行三个 `FixedTestArea*Test` 入口，记录上索、4/4/2 次接力、末架下索后的路径和最终站位；如任一路线失败，先按具体架序／地面段修正。三条独立空跑确认后，再把三个终点接入正式抢单和装箱自送双入口；试验园区仓储取货仍保持上游步行路线。

### 2026-09-19 接单实测正常，下一阶段准备试验园区

- 博士反馈上轮“武陵城固定滑索接单实测”测试没有问题，并决定后续开发试验园区路线。本机只读核对最近日志：`go-service.log` 于 16:35:47 将任务文本“把货物尽可能完整地交给普里莫·林德”解析为材料研究所 `deliver_target_map02_lv002_02`，`zip=true`、`fixedZipline=true`，选中 `AutoDeliveryRouteDestinationDeliverTargetMap02Lv00202WithFixedZipline`；`maafw.log` 中该固定导航节点于 16:37:12 动作成功，`SeizeDeliveryJobsMain` 于 17:04:42 返回 `Tasker.Task.Succeeded`。这与博士反馈一致。当前可读日志没有显示苏白易、观测站、猫头鹰在本轮分别完成真实接单，因此不将这三条或装箱自送、持货恢复、异常矩阵记为逐项实机通过；此前已确认的苏白易正常取货送货与材料研究所独立空跑保留。
- P5 起点仍是当前新版仓库的共享 AutoDelivery / MapNavigator，不恢复旧 MapTracker。当前 `fixed_zipline_routes.json` 只有武陵城固定路线；试验园区仓储 `domain_2_lv005_depot_1` 在 AutoDelivery 源配置中仍是 `walk_only: true`，三个终点为赵昭 `deliver_target_map02_lv005_01`、裴令容 `..._02`、阿禾 `..._03`，尚无新版固定滑索架序。旧路线的取货单跳及三个终点连滑 2／4／4 次 E 只作历史线索，不据此生成当前可执行路线。
- 下一次先核对当前账号试验园区的设施快照（地图、Level、类型、世界三维坐标、供电和连线），逐条确认取货与三个送货终点的完整有向架序、分段／E 提示、仓储到首架与下索后录制步行段、站位和面向点；若与旧资料一致也须重新唯一匹配并确认实际方向。随后按 [固定路线接入流程模板](./固定滑索路线接入流程模板.md) 先做独立空跑，再接入两个真实业务入口；现有 `walk_only` 取货路线在新路线通过前保持不动。试验园区实机结果和本轮武陵城结果分别记录。
- 本次只更新进度与接力说明，未修改运行资源、二进制、账号快照或原始日志，也未重跑代码检查。开工本地与 `myfork/codex/fixed-zipline-delivery` 一致、子模块锁定指针未变；仅本机 `outputs/` 未跟踪，不纳入提交。

### 2026-09-19 四终点接单实测准备与苏白易录制离索段

- 博士确认材料研究所下索后的修复版空跑没有问题；这是该路线的空跑结果，不扩展为四条路线实际交付通过。博士提供苏白易最后下索后的 `Wuling_Base` 录制路径：从 `[537.86,1269.57]` 到 `[538.75,1250.66]`，共 14 个地面点。
- 苏白易生成源的固定路线增加 `fixed_departure_path`，保留全部录制点顺序，末点按严格到达处理；不臆测额外交付朝向，普通步行／自动滑索节点与原有目标点不变。离线重新生成 `Routes/WulingCity.json` 后，四条终点的固定节点都携带各自的离索段，执行侧沿用上一轮已编译安装的实测落点接入机制；滑索架序与其他三条路线数据不变。安装资源指向本机源码，C++／Go 业务代码未改，无须重新编译 Agent。
- 原接单实测任务 `FixedWulingSubaiyiDeliveryTest` 保留内部 ID、正式接单入口和选项以兼容既有 MXU 设置，五语言显示名称改为“武陵城固定滑索接单实测”；开关说明同步列出四终点。实测需在任务选项中选武陵城、目标终点、完整送货，确认风险、开启滑索偏好及其下的固定滑索开关（两者默认关闭），建议次数选 1；否则依默认配置不会执行固定送货。
- 本机验证：AutoDelivery 生成器 24 项、原生离线规划 13 项、实际任务选项合并 1 项、`maa-tools check` 六组资源、`maa-tools test` 1279 项、Pipeline/Task Schema 均通过；DeliveryJobs 29 项中 28 项通过，仍有旧任务详情节点名断言失败（实际 `AutoDeliveryInDeliveryMissionDetail`，旧预期 `AutoDeliveryCheckDeliveryMissionSelected`），与本轮路线无关。资源检查使用临时 MaaFramework 5.13.0 配置并已清理。以上均非实际接单交付验收；下一步由博士依次观察四终点订单选择、下索轨迹、交付提示和任务退出。

### 2026-09-19 材料研究所下索后按实测落点接入录制步行段

- 博士反馈材料研究所滑索段正常、下索后先被错误预规划路线带走并绕路卡死。本机日志和 navmesh 对照：末架名义位置 `[474.75,1721.25]` 到录制首点 `[473.58,1718.96]` 会生成约 13 个绕行点；实测下索位置 `[474.44,1720.03]` 到首点则直达。原规划把前者当离索执行前缀，录制点虽保留却要等绕行走完才执行。
- 固定路线带 `fixed_departure_path` 时仍在移动前验证链尾可达，但不把名义位置生成的离索折线装入执行队列。末架确认并下索后停止输入，取得稳定、新鲜且在网格上的实测定位，从脚下接到录制首点，再顺序执行余下录制点和最终朝向；定位超时或无法接入时明确失败。无录制离索段的苏白易／取货路线及普通自动滑索不改执行方式。首点为 NAVMESH 时也只保留投影后的锚点，避免把名义起点折线重新带入。
- clean 后完整构建并安装 C++ Agent，最终 SHA256 `53955DFCDF04E0A59FE448596731469B1842635BE91E39D588BBD194A1B0AAC6`；Navigator 已恢复 `http://127.0.0.1:8770/`，load-status ready。原生离线规划 13 项（含名义／实测落点对照、无录制路线回归）、固定 C++ 契约、配置工具 6 项、`maa-tools check` 六组、`maa-tools test` 1279 项、Pipeline/Task Schema、`git diff --check` 均通过。资源检查使用临时 5.13.0 配置，已清理；第一次直接调用 cmake 因当前终端未加载 MSVC 标准库环境而失败，随后调用现有 MSVC 初始化函数完成构建。没有执行游戏输入或实机空跑，不宣称材料研究所步行已验收。
- 本轮优先请博士用材料研究所独立空跑入口验证末架下索、首点接入、逐点步行与最终站位。若仍绕路，保留新日志中的 `fixed_departure_rejoin`、实际定位和 `Dynamic navmesh overlay selected` 记录以定位；正式订单交付仍需单独验收。
- 修复保存点 `f51c884a` 已通过本机 127.0.0.1:7897 代理推送，`ls-remote` 核对个人分支与本地一致；此前未推送的 `94048e38`、`df6bcc0e` 也随之同步。仅本机 `outputs/` 未跟踪且未处理；本条接力状态补记随下一文档提交推送。

### 2026-09-19 材料研究所与猫头鹰离索路径更新

- 博士更新飞书“武陵城固定滑索路线填写表”并以红字标记变更。本机以用户身份读取“路线填写” A1:T24 和 revision 135～150 的变更记录，确认红字单元格为 B8、B13、B15、D13、D15；C 列观测站及其余字段未变。在线表格和账号信息仅留本机，不提交。
- 材料研究所：末架下索前按 B8 转向 294°；B13 的离索录制点替换为 `[473.58,1718.96]` 起、`[462.71,1713.21]` 止；B15 将最终站位定为 `[462.71,1713.21]`，交付面向点为 `[461.93,1713.43]`。保留严格到点与最终 `HEADING`。
- 猫头鹰：D13 的离索录制点替换为 `[510.94,1652.51]` 起、`[513.33,1652.14]` 止；D15 站位与“交付点”均为 `[513.33,1652.14]`。同点不能定义 P→T 朝向，因此只保留末点严格到达，不把零向量送入 `HEADING`；101° 架上下索朝向及原滑索架序不变。若实机交付需要特定朝向，仍须补充不同于站位的面向点。
- 修改固定路线数据、AutoDelivery 生成源与武陵城运行资源；普通路线、公共起步段、观测站与苏白易均不改。离线生成与检查通过不等于游戏已走通，下一步用正式固定入口观察两条下索后轨迹、站位与交付提示。
- 本机验证：AutoDelivery 23 项、原生离线规划 11 项、配置工具 6 项、固定 C++ 契约 1 项、设施快照匹配、`pnpm check` 6 组资源、`pnpm test` 1279 项、Pipeline/Task Schema、涉及文件格式与文档 lint 均通过。DeliveryJobs 29 项中 28 项通过；剩余旧断言仍期望 `AutoDeliveryCheckDeliveryMissionSelected`，实际为 `AutoDeliveryInDeliveryMissionDetail`，不将其改写为通过。Schema 首次因本机 GBK 不能打印校验符号而中断，设置 `PYTHONIOENCODING=utf-8` 后通过。未重新编译生产 Agent，也未执行游戏测试；本机运行资源为源码联接，换电脑仍需自行重建环境。
- 路线与测试已提交本地保存点 `94048e38`；收尾时 GitHub 直连 fetch 连接重置、重试超时，个人远端 push 遇 443 连接失败。本机配置的 127.0.0.1:7897 代理端口未监听，未改全局 Git 配置。当前本机提交尚未核实推送成功；另一台电脑接力前先恢复网络、fetch 并比较两端，不能假定已同步。

### 2026-09-18 实测反馈后收尾：滑索段正常，交付步行段待修

- 博士反馈：“实测滑索段没有问题，但交付的步行段还是有点问题，我们后面再修，先收尾更新吧”。本轮只登记结果、更新下一步并保存接力，不继续修改路线、分析日志或发起游戏测试。
- 本次实测滑索段按博士反馈记录为正常；交付步行段保留为已知问题，完整交付闭环尚未验收通过。博士未逐条列明本次测试覆盖的目的地、具体异常位置和表现，因此不推断三条路线均已逐项通过，不单独宣称 101° 下索、最终站位或交付出口已验收。
- 功能保存点为 `20db0abe`，已包含材料研究所、观测站、猫头鹰固定架序、首尾录制地面段、最终朝向与正式任务接线。后续先定位受影响终点及交付步行段，再依据对应日志／实录修补；保留已反馈正常的滑索段，不重复要求提交已有架序和地面录制资料。
- 当前家用新版目录 `D:\Github project\MaaEnd V2`，收尾开工时已 fetch 并确认本地与 `myfork/codex/fixed-zipline-delivery` 一致；tracked 文件干净，仅本机 `outputs/` 未跟踪。本次只提交接力及验证说明，账号资料、原始日志和输出文件不提交。
- 前序自动化与程序指纹继续以 [三条路线接入记录](./武陵城其他三条固定路线接入记录.md) 为准；此次仅检查 Markdown、链接和 Git 差异，未重新编译、运行全量测试或更改运行中的程序。

### 2026-09-18 接入武陵城其余三条固定送货路线

- 博士已填写在线表格，明确要求按此前流程补齐三条路线、接入任务后实际接单测试。完整读取在线“路线填写” A1:T200，revision 135；账号／设施未变、沿用当前快照、公共首架前地面段复用已确认苏白易录制。手动结果与交付提示仍未填写，不据此宣称实机通过。
- 新增材料研究所 `wuling_city_lind`（11 架、左键后 9 次 E）、观测站 `wuling_city_yushi`（9 架，#0～#5 / #5～#8，4＋2 次 E）、猫头鹰 `wuling_city_recycle`（12 架，#0～#9 / #9～#11，8＋1 次 E）。第二段从上一段末架重新瞄准，猫头鹰末架确认后转向 101° 再下索。全部新架子与当前快照唯一匹配。
- 三条路线已注册到 AutoDelivery 生成源与运行目录，抢单／装箱共用固定分发。沿用默认关闭的独立固定开关、全局 Never 步行分流、未配置终点自动规划；普通路径和站位修正不携带固定参数。新增三个独立空跑任务，复用正式固定节点，五语言及 interface 已同步。
- 原生导航新增固定专用首尾地面路径，先完成整条路线展开验证，再执行；录制点和最终 P→T 朝向保留。下索朝向复用架上闭环转向，仅作用于配置的末架，不增加定时按键。详见 [三条路线接入记录](./武陵城其他三条固定路线接入记录.md)，其中保留完整架序、入口、字段、证据与复现命令。
- C++ 从 clean 后完整构建并安装，Navigator 已重启且 ready；安装资源全部指向当前新版仓库，interface 为源码硬链接。最终产物指纹见上述接入记录。Go 仅更新分发测试，业务源码未改；构建脚本同步重编译 Go。
- 自动化：原生离线规划 11 项、固定 C++ 单元、Go 分发、选项合并／提示节点、AutoDelivery 22 项、6 组资源、Schema、1279 项节点测试通过。全部送货生成器 51 项中 50 通过，剩余仍为原有任务详情入口旧断言，未改断言掩盖差异。maa-tools 本轮以临时配置固定到已缓存 5.13.0；默认 latest 的新版本下载已终止，不更新安装目录框架。
- 开工分支与 `myfork` 一致，仅本机 `outputs/` 未跟踪。原始表格、账号快照、日志、产物和缓存不提交。下一步博士开启正式任务的“送货时优先使用滑索”及“使用已配置的固定滑索路线”后测试三个终点；重点确认两处停架重瞄、101° 下索、终点站位和交付出口。无须重新采集已提交的三条路线。

### 2026-09-17 GitHub 重试成功

- 博士要求重试 GitHub。直连仍然超时；检查发现 Windows 已启用本机 HTTP 代理，本轮通过命令级 `git -c http.proxy=...` 使用当前代理后，fetch / push 均成功，未更改全局 Git 配置。
- 远端没有独有提交，本地领先的采集填写说明与凌晨收尾记录已一并推送。已用 `ls-remote` 核对本地和远端均为 `ffe0a10f`，上次记录的未同步状态已解除；本条同步补记另行提交推送。
- 后续遇到相同直连超时，先检查当前电脑的实际代理设置，再通过命令级配置使用；不要假定另一台电脑使用相同端口。个人 Excel 仍只保存在本机 `outputs/`，未加入提交。

### 2026-09-17 凌晨收尾：飞书采集表接通，首架前公共段已确认

- 博士要求收尾并更新当天进度。本轮仅整理和保存接力资料，不继续开发新路线或发起游戏试跑。当前本机为 `D:\Github project\MaaEnd V2`，分支 `codex/fixed-zipline-delivery`；代码保存点 `5ab74a2f`，采集填写说明保存点 `7f705a67`。
- 9 月 16 日已完成：苏白易取货送货实测结果登记；正式接单测试入口选项修补；抢单 / 装箱自送独立固定路线开关、五语言、普通自动规划与固定节点分离；业务全局 Never 的启动前步行分流。新开关默认关闭，使用固定路线须在“送货时优先使用滑索”下显式开启。
- 博士不方便编辑 Markdown，已将填写表整理为 Excel，再以用户身份导入飞书电子表格，标题为 **武陵城固定滑索路线填写表**，包含“路线填写”“路线总览与方法”两页。导入后已回读两页内容，核对黄色填写区、区域备注、冻结首行列和保留的空白输入格。后续以飞书在线填写为当前资料，Markdown 只作字段说明，初始 Excel 不覆盖在线新增内容。
- 飞书官方 `lark-cli 1.0.95` 已全局安装；`lark-shared`、`lark-sheets`、`lark-drive` 三项官方技能已安装。应用配置、用户授权和 `auth status --json --verify` 均已通过，实际以 `--as user` 完成导入及读取。另一台电脑需要自行安装并确认登录态，不能复制或提交本机凭据。
- **新增路线证据：** 本轮收尾回读飞书“路线填写”页 A6:E24，结果未截断。B6 写明“复用苏白易（武陵城固定滑索送货路线全都共用这一段）”，C6 / D6 同样为“复用苏白易”。因此材料研究所（左下，普里莫·林德）、观测站（右上，于施）、猫头鹰（右下，资源回收站）的仓储至首架地面段均已获博士确认共用苏白易，不再列为待重录项；这不等于三条滑索架序或连滑范围也相同。
- 上述三条路线的完整架序、连滑区间、段间衔接、下索后地面路径、站位朝向和手动跑通结果仍为空；账号 / 设施是否变化和快照时间也尚未填写。暂未接入其他终点的新固定路线。真实装箱自送、持货恢复与异常矩阵按博士指示延后，不因缺订单阻塞继续采集。
- Navigator 已从当前新版目录启动，地址 `http://127.0.0.1:8770/`；页面 HTTP 200，导航 Agent 连接初始化完成。收尾核对时仍正常运行，保留供博士录制；启动成功不等于路线录制或游戏验收通过。
- 私人表格定位信息保存在本机 `.cache/feishu-cli/route-sheet.json`，初始 Excel 位于 `outputs/01a0aac5-6444-7802-8fd7-323a42c5db33/`；两者及认证资料、授权二维码、日志均不纳入 Git。另一台电脑可用同一用户身份按上述准确表名定位，不把个人飞书链接或账号标识写入公开仓库。
- 前序代码验证结果保持：Go 分发、实际框架选项合并、9 项原生规划、资源 / Schema、1279 项节点测试通过；生成器 50 项中 49 项通过，仍留有任务详情旧断言失败。此次收尾只检查文档格式、链接与差异，未重跑编译或全量测试，三项子模块指针未变。
- 收尾开工时 tracked 文件无未提交改动，仅 `outputs/` 为本地未跟踪产物。本轮 fetch 及两次 push 均因 GitHub 443 连接超时失败，无法核实远端当前进度；采集说明与本次收尾记录已提交本地，尚未确认同步。下一次接力先恢复网络并比较 / 推送本地分支，不假定另一台电脑已收到今天的文档。

### 2026-09-16 整理武陵城路线采集填写表

- 博士要求回顾提供路线的方式，并按区域备注列出需要补充的信息。已新增 [武陵城路线采集填写表](./武陵城固定滑索路线采集填写表.md)：公共取货与技术生产办公室（左上，苏白易）保留已通过状态，本轮待填为材料研究所（左下，普里莫·林德）、观测站（右上，于施）、猫头鹰（右下，资源回收站）。
- 表格区分地面路径 JSON、完整架序、连滑范围、段间衔接、站位朝向和障碍；允许直接注明复用苏白易共用段，技术 ID / 快照匹配由助手处理。无订单交付提示继续待确认，不要求重录已通过路线。
- 本轮仅新增 / 更新文档，检查 Markdown、链接与差异；未改代码、未重跑编译或游戏测试。

### 2026-09-16 暂缓订单实测，完成双入口固定路线开关

- 博士明确当前没有订单，先跳过实测继续后续开发。装箱自送、已有委托、持货恢复和异常矩阵保持待验收，不要求先拿到订单才能推进开发。
- 开工 `613f36d4`，本地干净，fetch 后与个人远端一致；三项子模块锁定指针不变。其余武陵城终点仍只有旧版分段端点与接力次数，缺少当前完整架序，未凭旧坐标生成可执行新路线。本轮先完成可独立开发的 P4 路线选择。
- 抢单与装箱自送在“送货时优先使用滑索”下均增加“使用已配置的固定滑索路线”，默认关闭，五语言齐全。开启固定需要 Win32-Front；保留原风险确认与调用方业务流程。已有任务配置需显式打开新选项，否则恢复普通自动滑索规划；独立取货、送货与连续空跑测试不变。
- 生成器为已配置路线新增 `WithFixedZipline` 节点及目录 `fixed_route_node`；普通 `WithZipline` 不再携带固定 ID。Go 共享分发优先遵守步行选项，固定开启时选择武陵取货 / 苏白易固定节点；未配置终点继续自动规划，站位修正始终独立。业务全局 Never 在移动前选择步行并记录 `global_zipline_never`；独立固定测试仍严格拒绝禁滑。缺设施、歧义、坏配置和固定段失败仍停止，尚未实现其他设施预检降级。
- 修改生成源后重新生成 AutoDelivery 目录 / 路线和 DeliveryJobs 任务；后者同步补回模型已有的香焗兽肝 8 个优先级选项，修复先前 241 / 242 数量不一致。没有改装箱业务流程、物品模型或源目录数据。接口文档旧 `custom_action_param.zip` 示例改为实际读取的 `attach.zip`，路线接入模板同步独立固定节点契约。
- `go test ./autodelivery` 通过（全目录分发矩阵、站位修正独立、选项解析、全局禁滑）；实际 MaaFramework 双任务选项合并 / 切换测试通过；9 项原生规划、`pnpm check`、Pipeline/Task Schema 和 1279 项节点测试通过。生成器 50 项中 49 通过；剩余一项为旧任务详情入口断言（期望 `AutoDeliveryCheckDeliveryMissionSelected`，当前为 `AutoDeliveryInDeliveryMissionDetail`），保持记录，不冒充全量通过。
- 新 Go Agent 已通过 `.venv/Scripts/python.exe -m tools.setup.build_and_install` 编译安装；SHA256 `B176FA9080524D8AF32969B0FCE44955A6E3361D26698935B32760A2ACA3B508`。C++ 未改、未重编，指纹保持前轮。安装资源链接已重建；需重启 Agent / 重新加载资源后使用新开关。本轮无游戏输入、无订单实测；首次单独 Schema 命令漏设 UTF-8 导致终端编码异常，重新加载 Enter-Dev 环境后验证通过。
- 操作、行为边界与延后的实机项目见 [双入口与恢复验收说明](./苏白易双入口与恢复验收.md)。下一步继续补武陵城其他终点当前架序及首尾地面录制；完整设施预检降级仍为后续代码工作，订单恢复后再补真实业务矩阵。

### 2026-09-16 苏白易取货送货通过，推进双入口与恢复验收

- 博士确认“已实测苏白易取货送货没有问题，可以进入下一步”。本轮按用户反馈登记正常业务链通过，不再重复要求正常空跑；未提供本轮日志，订单来源、耗时、货物损伤与最终循环出口不补写为已核验。
- 完成共享组件分发、抢单和装箱入口、已有委托及失败出口代码核查，新增 [双入口与恢复验收说明](./苏白易双入口与恢复验收.md)。下一项实机为正式 DeliveryJobs 的苏白易装箱自送及交付后返回武陵循环；随后补持货恢复和异常场景，再迁移其他终点。
- 修复 `FixedWulingSubaiyiDeliveryTest` 缺少任务选项：复用正式抢单的奖励、来源、后处理配置，用户可在该入口配置完整送货、既有风险确认、滑索偏好与次数。入口不会自动筛选苏白易，也不会默认启用全自动送货。
- 原生规划测试原来把所有 path 点当成滑索架，且对新增 ZONE 设置 required，导致两项过时断言失败；现改为从架序数据验证每跳起终点，插入真实必经动作反例，并补生成武陵取货单跳/零 E 校验。9 项原生规划、6 项配置工具、`pnpm check`、Pipeline/Task Schema、1279 项节点测试通过。生成器共 49 项，47 通过，2 项仍为已记录的历史失败，未改无关装箱代码。
- 当前仍通过 `attach.zip` 选择普通 / WithZipline 路线，没有独立的“上游自动滑索 / 优先固定路线”选项；启动前业务降级、完整恢复矩阵和其他终点仍未完成，不将本轮结论扩大为 P4 全部通过。
- 本机 `D:\Github project\MaaEnd V2`，开工 HEAD `0f55272c`、工作区干净；fetch 首次连接重置，HTTP/1.1 重试成功，本地领先个人分支 14 提交，无远端独有提交。子模块已同步且三个锁定指针不变。本轮未修改 Go/C++ 生产代码，未重建 Agent；C++ SHA256 仍为 `C6A7C8956D9A9F1D4362CF8CE27D920D4AA67E11023D46A5B68934ADAA9FEB44`，安装 tasks 为指向源码的 Junction，重新加载资源即可使用任务改动。
- 尝试指定仓库内 pnpm store 同步依赖时，因现有 node_modules 使用 `D:\.pnpm-store\v10` 且非交互重建被 pnpm 拒绝而退出；未强制删除可用依赖，后续检查使用现有已安装依赖。本机仍未找到 uv，不声称完成 Python 锁定依赖重同步。原始日志、快照、账号配置和缓存不提交。

### 2026-09-16 整理后续路线接入模板，暂停开发

- 博士建议在迁移武陵城其他终点前，先整理可复用的路线开发流程。已新增 [固定滑索路线接入流程模板](./固定滑索路线接入流程模板.md)，明确范围、设施/地面/朝向数据准备、配置接入顺序、静态检查、无订单实测、真实业务验收和收尾记录。
- 今天完成：武陵城固定取货、苏白易固定送货、取货送货空跑、正式接单测试入口，以及送货首架地面路径优化。正式真实订单闭环尚未执行。
- 后续顺序调整为：先用新模板完成苏白易真实订单闭环和异常矩阵，再按模板接入武陵城其他终点，最后迁移试验园区。今天到此收尾。

### 2026-09-16 新增正式接单实测入口

- 新增 `FixedWulingSubaiyiDeliveryTest` 任务入口，复用正式 `SeizeDeliveryJobsMain` 流程，包含接单、取货识别、武陵城固定取货、送货目的地识别、苏白易固定送货和交付识别。
- 该入口不复制业务 Pipeline，控制器限定为 Win32-Front；任务选项沿用正式接单任务的默认行为。需要调整来源、目的地或收尾策略时，仍使用正式 `SeizeDeliveryJobs` 任务的对应选项。
- `pnpm check` 通过。下一步是博士接到苏白易委托后，用该入口完成一次真实订单闭环并记录取货、送货识别、固定路线和交付出口。

### 2026-09-15 优化苏白易送货首架前地面路径

- 博士反馈空跑主链正常，但仓储到苏白易第一架之间的旧步行段容易被设备阻挡；已用 Navigator 新录制的路径替换固定送货测试入口的首段：`[952.84,1832.95] → [953.86,1832.9] → [955.75,1832.86] → [957.7,1832.87] → [959.55,1832.83] → [961.39,1832.63] → [961.28,1831.17]`，并保留后续固定架序路径。
- 取货送货空跑任务复用 `MapNavigatorFixedSubaiyiTest`，因此同步获得该路径更新；武陵取货段和后续滑索架序不变。
- `pnpm check` 通过；尚未对这条优化后的送货首段重新进行实机空跑。

### 2026-09-15 武陵城固定取货无订单实机通过

- 博士使用新增的 `FixedWulingPickupTest` 进行无订单实机验证：首架到第二架滑索段运行正常，未执行取货交互。
- 下索后的最终站位和取货朝向落点均符合预期，确认现有仓储交互方向可直接复用；本轮不再追加额外 HEADING 节点。
- `wuling_city_pickup` 已绑定武陵城仓储的 WithZipline 共享 AutoDelivery 入口，普通步行入口和苏白易送货固定路线保持独立。至此武陵城固定取货已完成无订单局部验收，下一步可进入真实取货/交付双入口接线与恢复矩阵。

### 2026-09-15 Navigator 实录确认武陵城取货两架并接入固定路线

- 博士使用当前 MapNavigator 录制了武陵城取货路线。首架到第二架已确认：`(-783,277,-577)` → `(-766,279,-649)`，为单跳、零次 E；首架与此前由 base 坐标反算出的候选一致。
- Navigator 实录的传送后接近段从 `Wuling_Base` 的 `[941.78,1778.6]` 开始，末端经过 `[950.07,1777.27]`；下索后路线经过 `[960.65,1828]` 至 `[952.92,1832.79]`。仓储标准目标已有现成配置 `[952.2,1832.68]`，本次不覆盖公共路径，保留录制作为当前路线证据。
- 新增固定路线 `wuling_city_pickup`，架序为上述两架；武陵城仓储 WithZipline 节点已绑定该 ID。普通仓储路线、苏白易送货路线和仓储站位修正节点保持原语义。
- 生成器固定路线测试 20 项通过，`pnpm check` 通过；尚未进行固定取货无订单实机运行，因此本提交只证明配置、生成和资源协议正确，不宣称上索、落架和取货交互已验收。

### 2026-09-15 资料核验：当前设施快照已定位，取货路线仍缺实机证据

- 当前工作区仍干净，分支与 `myfork` 一致；未修改业务代码。
- 当前安装目录存在账号 `a3914272c8ad74b4` 于 2026-09-13 导入的 `install/debug/record/Ziplines.json`。快照包含 `map02_lv002` 的 643 个设施标记，保留 `level_id`、`template_id` 和三维 `x/y/z`，因此可以作为新版设施匹配的唯一数据源。
- 已确认武陵城取货所在 `map02_lv002` 的当前设施标记中存在多组同模板邻近架，不能仅凭旧版两个目标或旧 MapTracker 坐标直接选架。当前固定路线数据 `assets/data/MapNavigator/fixed_zipline_routes.json` 仍只有苏白易 `wuling_city_subaiyi`，尚未注册取货链。
- 旧版取货材料仍只提供 base 坐标 `[949.74,1777.67]`、`[951.98,1832.68]` 和 MapTracker 目标 `[682.9,785.25]`；这些坐标系不能与当前设施快照直接混用。传送后安全通道、两架唯一匹配、取货末架接地段及当前仓储交互站位仍需实机/地图工具证据。
- 因此本轮完成了 A 阶段的源码与快照核对，但尚未满足进入 B 阶段的证据门槛；下一步应使用当前地图工具从快照筛出候选架并由实机确认首末架、P/T 和安全通道，之后再写固定取货配置。
- 已运行 `tools/fixed-delivery/validate_fixed_routes.py` 对当前快照校验：苏白易 16 架全部唯一匹配，15 跳及 14 次 E 预算一致；脚本明确不验证供电和实时连线。武陵城取货没有可供同一脚本验证的路线配置，因此不能据此进入 B 阶段。
- 依据当前 `map02base` 仿射 `nav_x=0.75*sx+1536`、`nav_y=-0.75*sz+1344` 对旧版武陵取货首架 base 点 `[949.74,1777.67]` 反算，得到世界坐标约 `(-781.68,-578.23)`，与当前快照滑索架 `(-783,277,-577)`（苏白易路线候选中的 #1）相符。这只能确认首架候选，旧版“第二架”目标未提供可转换的新世界坐标；其相邻候选至少包括当前 #0（约 74m）和 #2（约 103m），方向与实际连接仍需实机确认。
- 按当前滑索架类型的 110m 最大跨度，对首架 `(-783,277,-577)` 做几何邻接筛选，得到 3 个同层同模板候选：`(-766,279,-649)` 74.01m、`(-793,290,-678)` 102.32m、`(-786,276,-474)` 103.05m。该结果只代表可能连线，未验证实际连接方向、供电或可上索状态，暂不生成路线。
- 用当前快照中的供电标记按水平 30m 半径筛选，首架及 3 个候选均有供电结构覆盖：首架邻近 `(-786,277,-581)` 息壤供电桩；3 个候选分别邻近 `(-772,279,-649)` 息壤中继器、`(-790,290,-679)` 息壤供电桩、`(-789,276,-474)` 息壤供电桩。供电覆盖仍不能证明两架之间存在实际滑索连接。

### 2026-09-15 真实送货前置调研：固定取货与朝向

- 收尾指令：博士要求“更新调研结论，下次继续开发”。本次仅更新文档并保存，不开始业务实现。下次先检查当前设施与已有采点材料，核验取货两架、安全地面通道、架前／仓储／苏白易站位 P 和朝向点 T；材料齐全后直接推进导航改造，缺少实机证据时只补缺项。
- 博士要求在真实送货闭环前，先调研旧版固定取货滑索，以及取货点、送货点和滑索架的两点 heading。已形成 [调研与改造方案](./固定取货滑索与朝向改造调研方案.md)，本轮未修改业务代码或重新进行游戏验收。
- 本机源码基线 `df3186bb`，开工时工作区干净；三项子模块保持锁定提交，C++ 程序指纹与连滑实测一致。旧版只读依据为本地已有 `649ca850`；本轮 fetch 网络超时，未据此宣称远端最新。
- 旧版武陵城／试验园区取货确为单跳滑索，但两条取货末端的 HEADING 在该提交仍是预留注释；苏白易交付及部分架前朝向有实际配置。两类证据分别记录。
- 新版已有两点算角和架上闭环。主要缺口为固定路线首尾地面约束、固定 path 不接受 HEADING、普通 HEADING 在反馈缺失或超差时仍可完成，以及地面转向脉冲后的站位复核。
- 下次开发方向为武陵城固定取货单跳及地面约束、严格朝向和站位确认，独立无订单实测通过后，再进入双入口接线与真实送货。拟议字段及角度容差尚未定稿，按实现和实测证据确认；所需点位与界面材料见新文档。
- 本轮三个文档通过 Markdown 检查，16 个本地链接及 Git 空白差异核验通过；未重新编译、执行 `pnpm check/test` 或实机测试。
- 调研期间 fetch 与首次 push 因 GitHub 网络超时失败；随后重试成功，调研及同步补记已保存至 `d70e35a5`，本次收尾开始时再次确认与远端一致。本次结论更新另行提交并核验推送，不更改已通过实机验收的程序。

### 2026-09-14 当前保存点：苏白易整段连滑实机通过

- 博士反馈“连滑没有问题，保存并更新进度”。本轮源码 `a5231ea3`，本地无未提交改动，个人远端一致；三个子模块仍为锁定提交。
- 日志验证 1 次首跳发射、14 次 E / 14 次提示消费，途中无逐架定位；随后完成末架确认、自动下索和最后步行，框架任务成功。总耗时约 107.2 秒，详见 [连滑实机验收记录](./苏白易连滑实机验收记录.md)。
- 苏白易 16 架 / 15 跳、首跳左键后 14 次 E 的正常移动链验收通过。前一轮等待第 15 次提示导致超时的问题已修复并实机验证，不再安排同一问题的重复验收。
- 本轮仅保存验收文档，不改业务代码；原始日志、账号配置和安装产物留在本机。此前软件检查结果保留，不把文档收尾说成本轮重跑全量测试。
- 当前按博士要求保存进度；真实取货/交付、抢单与装箱自送双入口、武陵城其他终点、试验园区继续暂缓。P0 上游步行/自动滑索对照和异常场景完整矩阵仍未全部完成，不将单条路线正常空跑扩大为整个项目验收。

### 2026-09-14 修复计数：首跳左键后 14 次 E（历史保存点，现已复测通过）

- 博士明确接受按“区间架数减二”修正：苏白易 16 架 / 15 跳，首跳左键后 14 次 E。途中仍不逐架定位；预算完成且最后提示消失后，进入末架确认，再自动下索走向终点。
- 将区间跳数与 E 次数分开：区间跳数保留 `last - first`，E 次数为该值减一；2 架单跳预算为零，无连滑时防止无符号下溢。运行时、预览和离线校验输出保持一致。
- 已 clean 完整重建并安装；C++ 计数/边界/去重测试、6 项配置工具、8 项原生规划（保留 15 跳且 E 预算为 14）、`pnpm check` 和 1279 项节点测试通过。本轮修改不等于实机收尾已通过；下一步用同一“固定滑索测试”复测末架确认、自动下索和最后步行段。

### 2026-09-14 连滑首轮：已到末架，E 预算多一次

- 博士反馈连滑正常，到末架停留后人工下索。日志核对为 14 次 E / 14 次提示消费，程序仍等待预算中的第 15 次，30 秒后 `zipline_relay_prompt_timeout` 自行失败；末架定位阶段未启动。
- 结合实机反馈，苏白易应为首跳左键后 14 次 E。修复方向是将通用段预算修正为 N 架对应 N−2 次 E；诊断时尚未改代码；博士随后同意修复，当前进度见上方修复记录。
- 证据与时序见 [连滑首轮日志分析](./苏白易连滑首轮日志分析.md)。不能把这轮记为端到端连滑验收通过，也不能把自动超时归因于人工下索。

### 2026-09-14 实机验收及连滑确认

- 博士已完成补强版苏白易独立空跑并反馈无问题。日志核对确认完整 16 架/15 跳、15 次发射与落架对应、最终到达证据和任务成功，导航动作约 127.1 秒，详见 [实机验收记录](./苏白易固定逐跳实机验收记录.md)。不再要求重复空跑来满足旧三次门槛。
- 本路线 #0～#15 已确认作为一个完整连滑段，途中不判断下一架位置。末架仍单独定位确认，真实交付和其他路线继续暂缓。
- 首版历史约定：首次左键起滑不计入，之后再按 N−1 次 E（16 架 15 次，4 架 3 次）。首轮实测后已按上方记录修正为 N−2。#0～#15 整段连滑配置保留；途中只识别提示与计数，不判断下一架位置，段末仍单独定位。
- C++ 计数/分段/重启状态测试、2 项真实 Pipeline 合成截图测试（含停止前不发按键）、8 项原生规划、6 项配置工具、20 项 AutoDelivery 生成器测试通过；资源检查、Schema 和 1279 项节点测试通过。连滑尚未实机验收。
- 本机 Ninja 的 MSVC `/showIncludes` 中文前缀探测成乱码，曾漏编头文件依赖并使规划回归失败。已执行 clean 后完整构建，随后验证通过。以后头文件或结构体改动先 clean，再调用正常构建；没有更改系统语言或编译器安装。当前程序指纹及复现命令见固定路线 README。

### 2026-09-13 家里补强固定路线

- 当前目录 `D:\Github project\MaaEnd V2`，分支 `codex/fixed-zipline-delivery`；开工时工作区干净，本地与个人远端均为 `f066e64a`。
- 苏白易当前配置为 **16 架、15 跳（#0～#15）**。9 月 13 日补入后段架子后，旧记录中的 15 架/14 跳及重复 #15 编号失效；路线配置、预览与测试入口坐标对应。
- 已有一次独立实机空跑成功记录，架序及末架到苏白易终点正常。该结果来自上一轮；不能代表本轮补强代码已经实机通过。
- 本轮补齐固定参数 Schema、AutoDelivery 生成源与回归用例；固定路线 ID 不会因重新生成而丢失。已有苏白易业务节点接线保留，真实委托交付尚未验收。
- 固定路线按地图、Level、模板、三维坐标唯一匹配，缺架、歧义、坏配置均拒绝；只规划首架到末架的完整有向链，不按成本截取子链。固定参数当前用于单段移动路径，不能静默跳过必经动作或过图。
- 严格固定入口遇到全局禁滑、不可用数据或不可达路线时在移动前失败；固定上索/乘索失败停止，不下索换路、不重新跑整条作者路径。普通自动滑索保留上游恢复逻辑。
- 博士修正验收：**本轮由博士再空跑一轮即可，不要求连续三次**。连滑段需要按具体路线定制，分段待博士确认；暂不实现统一次数或全路线连滑。真实送货双入口及其他终点暂缓。
- 本机当前终端找不到 `uv`；使用已安装的 `.venv/Scripts/python.exe` 调用同一构建模块完成 C++ 编译安装，不据此声称重新同步过锁定依赖。

本轮验证结果、复现命令见 [固定路线开发与验证](../../../tools/fixed-delivery/README.md)。收尾以本地 HEAD 与 `git ls-remote myfork refs/heads/codex/fixed-zipline-delivery` 一致为同步依据，原始快照、日志、账号配置及构建产物不提交。

## 3. 基线和上游变化

| 对象 | 版本或位置 |
| --- | --- |
| 当前开发基线 | `01ef330ad467f00fa6208f06a0eafdb408fbfa58` |
| MaaUtils | `6e9ba33f6ad835418097d9324c01c44a82825a2b` |
| MaaEnd-AI | `746e0854cad7977a9ec672b338c168a547b0b577` |
| MaaEndTestset | `c800a4ef7a93a420e883b9d9e4541936b2d94d2b` |
| 上次计划调研基线 | `cc3b79c9cbc156ee30cf9ea6e3dbb594551750db` |
| 个人旧固定滑索分支 | `feature/zipline-fast`，调研证据 `649ca85082acf5e85f95ddb4376793b0045f0d71` |
| 早期个人自送分支 | `feature/self-deliver-route`，证据 `b1fccddd67c479eaa292d0fa6e04c1fadd4b8038` |
| 上游 | <https://github.com/MaaEnd/MaaEnd> |
| 个人新版分支 | <https://github.com/Hadrien-codeur/MaaEnd/tree/codex/fixed-zipline-delivery> |

昨日基线之后共 7 个提交。对本项目主要影响：

- `38c15b5d`：虚拟禁区编辑与规划器避让。
- `efacac15`：运行期虚拟禁区用于绕障恢复。固定段失败策略需与该重规划行为衔接。
- `f4c44dae`：MaaUtils 更新以适配 MaaFramework v5.13.0，不能沿用昨日的依赖指针。
- `d66b675f`：3D 工具轨迹高度及观察者模式；取路线证据应使用当前工具。
- `01ef330a`：动作策略集中到 `ActionTraits`；`AbandonZipline` 改用 `IsContinuousRun()`。后续架序扩展需参考新结构。
- 另两项为 PlayCover 输入适配和赠礼修复。

已核对 `navigation.go` 仍从 `attach.zip` 读取配置，`ZiplineStore` 仍按地图存储，没有账号隔离字段。以上是源码核查，不是实机结论。相关代码与依赖更新必须在 P0 重新验证；不要自动纳入未合并分支。

## 4. 公司环境与历史基线（2026-09-08～10）

| 工具 | 本轮状态 |
| --- | --- |
| Git | 2.53.0.windows.2 |
| Node | v24.14.1 |
| pnpm | 项目实际使用 10.28.0，遵循 packageManager；全局版本不作为项目版本 |
| Go | go1.26.2 windows/amd64 |
| Python | 3.13.13，本项目有独立 `.venv` |
| uv | 0.12.10，位于 `.cache/tools/uv` |
| Python MaaFramework | uv.lock 锁定 5.13.0b4；不等于安装目录 DLL 已验证 |
| 安装目录 MaaFramework | v5.13.0，已通过原生 Agent 连接与导航网格加载检查 |
| MXU | v2.5.2，程序 `install/mxu.exe` 已安装；未进行游戏任务验证 |
| MaaDeps | v2.12.6，按上游下载脚本安装 |
| WebView2 SDK | 1.0.2210.55，按上游脚本安装；2026-09-10 已由博士完成登录并导入设施 |
| Go Agent | 本机由当前工作分支构建，业务源码与 `01ef330a` 相同；初始化工具会再次编译 |
| C++ Agent | 2026-09-09 已由本机源码（业务基线 `01ef330a`）编译安装；早期使用官方构建 34144719594 |
| MSVC / CMake / Windows SDK | MSVC 19.44.35228、CMake 3.31.6 位于 `.cache/BuildTools`；Windows SDK 10.0.26100.0 位于系统 Windows Kits；本地编译已通过 |

当前终端环境设置：

```powershell
Set-Location 'E:\MaaEnd V2'
. .\tools\fixed-delivery\Enter-Dev.ps1
```

uv、Go 模块、Go 编译、npm 和临时目录都指向新版 `.cache`；pnpm 安装使用 `--store-dir .cache/pnpm-store`。这些设置不更改系统全局配置，换终端要重新执行。

安装来源和指纹：

| 文件 | SHA256 |
| --- | --- |
| `install/agent/go-service.exe` | `FDB94EC2B601522CE287BBE30832C66549685DF0F9D9411099B1E17BC097E1B9` |
| `install/agent/cpp-algo.exe` | `AC058D79A3B30126B3CC3A71DAA96A14EB2EE375B061AD3C588FCFD85212C8DE` |
| `install/maafw/MaaFramework.dll` | `D4503D525561A7BE46D7B2A5E64F3288F5B3BA7AAEFAF36AB44F7CE4C04CFFAE` |

`install/version.json` 的 `cpp_algo_sha256` 是下载 artifact 的摘要 `33d5b991916b36eef7ed1406c126de36c68ddf38d63dfccffd7298cd422cb91b`，不是上表 exe 文件的摘要。该版本文件仍保留下载信息，不代表随后本地编译的产物来源；以本轮编译记录和 exe 指纹为准。

已核实资源联接：`install/resource` 指向本仓库 `assets/resource`，`install/data` 指向 `assets/data`，`install/maafw` 指向 `deps/bin`。本版安装脚本将 `install/interface.json` 与 `assets/interface.json` 建为硬链接，已通过文件系统查询确认；这与上游部分文档的“复制文件”说明不同，应以每台机器的实际文件类型为准。

### 本轮验证结果

| 检查 | 实际结果 |
| --- | --- |
| 新增文档 markdownlint、Git 空白检查 | 通过 |
| 环境脚本 PowerShell 语法及执行 | 通过 |
| `pnpm check` | 通过，6 组资源配置，maa-tools 使用 MaaFramework 5.13.0 |
| Schema 校验，按上游 CI 指定目录范围 | 通过 |
| `pnpm test` | 通过，1279 项节点用例 |
| AutoDelivery / DeliveryJobs 生成器测试 | 44 通过、2 失败，共 46 项 |
| Go 本地构建 | 通过 |
| MapNavigator 启动、Agent 连接和区域查询 | 通过，load-status 为 ready，无错误，返回 84 个区域 |
| C++ 本地构建 | 通过，MSVC 19.44.35228、CMake 3.31.6，已安装 `install/agent/cpp-algo.exe` |
| 游戏内空跑和真实交付 | 未执行，已完成森空岛坐标导入，待按地图标记选择路线后实测 |

两项生成器失败均发生于未修改的上游文件；已用 `git diff 01ef330a` 核对送货生成器和 AutoDelivery 资源无差异：

1. `tools/pipeline-generate/DeliveryJobs/data.test.mjs:278`，物品选项数量断言在第 311 行得到 241，期望 242。后续只读对照确认：四号谷地和武陵的第一优先级选项均比模型数据少 `item_slwood_1_hshog_1_1`（香焗兽肝），没有多余选项。这是装箱选项完整性问题，不是已证实的滑索导航故障。
2. 同文件第 952 行的任务详情测试，在第 1186 行期望 `AutoDeliveryCheckDeliveryMissionSelected`，资源实际为 `AutoDeliveryInDeliveryMissionDetail`。后者同时检查 `InMenuMission` 和原有送货详情识别；表现为测试仍期待旧接线，不等同于已经证实游戏内卡死。此轮没有修改流程或测试断言。

当前仅记录原版基线差异，未修改断言使其强行通过。接入新版业务之前需复核生成源与用例，明确修复或更新上游的处理结果。

本机详细输出在 `.cache/check.log`、`.cache/schema-check.log`、`.cache/node-tests.log`、`.cache/delivery-generator-tests.log`、`.cache/setup-workspace.log` 和 `.cache/map-navigator-smoke.log`，这些日志不随 Git 同步。上面的结论、失败位置和重现命令已随文档保存。

## 5. 家里电脑首次接力

以下路径是示例，家里可替换成另一独立新版目录。目标目录需为空，不覆盖旧仓库。

```powershell
git clone --recurse-submodules --shallow-submodules --origin myfork --branch codex/fixed-zipline-delivery https://github.com/Hadrien-codeur/MaaEnd.git 'E:\MaaEnd V2'
Set-Location 'E:\MaaEnd V2'
git remote add origin https://github.com/MaaEnd/MaaEnd.git
git config remote.origin.pushurl DISABLED
git config remote.pushDefault myfork
git config push.default simple
git config pull.ff only
git status --short --branch
```

然后阅读本记录和规范，核对工具清单。GitHub 登录需要在该电脑独立完成，不复制另一台电脑的凭据。

若电脑已有 uv，可直接使用；否则在新仓库内下载本轮相同版本：

```powershell
New-Item -ItemType Directory -Force -Path '.cache/downloads','.cache/tools/uv' | Out-Null
gh release download 0.12.10 --repo astral-sh/uv --pattern uv-x86_64-pc-windows-msvc.zip --dir .cache/downloads
Expand-Archive -LiteralPath '.cache/downloads/uv-x86_64-pc-windows-msvc.zip' -DestinationPath '.cache/tools/uv'
. .\tools\fixed-delivery\Enter-Dev.ps1
uv sync --frozen
pnpm install --frozen-lockfile --store-dir .cache/pnpm-store
```

下载/解压命令仅用于首次安装；已有对应文件时先验证版本，不重复覆盖。随后按本记录实际完成状态补建环境：

```powershell
uv run --frozen setup-workspace --cpp-algo-run 34144719594
uv run --frozen build-and-install
uv run --frozen build-and-install --cpp-algo
```

`setup-workspace` 会下载依赖与预编译程序，可能选取 CI/Release；必须记录实际来源，不能自动认定与源码同一提交。涉及 C++ 修改后必须本地重新编译。资源目录联接只能指向当前新版仓库。

上面的 CI run 仅适用于本轮 `01ef330a` 业务源码基线；C++ 有改动后不得继续用该旧构建。CI artifact 也可能过期，需要本地编译或从已核实的同提交产物恢复。MaaFramework/MXU 下载脚本使用当时发布版本，家里安装后应与本记录对照，不能假定日期不同仍获得相同版本。

## 6. 日常接力与收尾

开工流程：先查看本地状态；有未提交改动时保留并辨明来源，再决定如何同步。下面 pull 和子模块更新只在工作区干净、子模块没有自有改动时执行：

```powershell
git status --short --branch
git fetch myfork
git log --oneline --left-right HEAD...myfork/codex/fixed-zipline-delivery
git pull --ff-only myfork codex/fixed-zipline-delivery
git submodule update --init --recursive
. .\tools\fixed-delivery\Enter-Dev.ps1
```

若分叉，先保留两侧提交并审查差异，不用强制覆盖。按变更更新依赖和重建 Agent，报告当前阶段、下一步后继续；总体方案已经确认，不反复要求重批。更新上游需要单独评估，不与日常同步混做。

收尾流程：更新本记录中的状态、验证、待办和环境，暂存本项目相关文件，以 `docs:` / `feat:` / `fix:` 等前缀提交，然后执行：

```powershell
git push myfork codex/fixed-zipline-delivery
git rev-parse HEAD
git ls-remote myfork refs/heads/codex/fixed-zipline-delivery
git status --short --branch
```

本地与远端提交号一致才报告已同步。安装包、日志和缓存不会随 Git 同步；游戏设施需要在新电脑按当前账号重新导入并验证。不要依赖旧聊天记录、临时目录或旧公司仓库才能继续。

## 7. 下一步与实机材料

1. 下次接力先核对 `codex/fixed-zipline-delivery`、`myfork` 和 `codex/fixed-delivery-upstream-adapter` 的提交号；当前稳定远端基线为 `a8c21918`，迁移保存点为 `81254d00`。不要把迁移分支直接合入稳定分支。
2. 使用已重建的候选 staging 复测林德单点，核对 `fired -> riding`、relay 启动、9 次 E、末架落地、下索朝向和离索路径；失败时先保存当前日志，不重复运行覆盖证据。
3. 林德通过后再各跑一个武陵城、试验园区终点烟测；随后使用正式 `SeizeDeliveryJobs` 与 `DeliveryJobs` 两类入口回归七个终点，记录 `source_id`、实际固定节点、每段 E／下索朝向、交付提示、任务退出和日志。
4. 在正常订单之外完成异常矩阵：固定开关关闭、全局禁滑、未配置终点、已有持货恢复、固定段失败停止。每项保留日志或截图证据，未完成前不覆盖当前安装。
5. 候选实机证据全部通过后，再做正式安装的原子切换与回滚演练；稳定版本、二进制、资源版本和提交号在切换前保持可恢复。试验园区仓储取货继续使用上游 `walk_only`，不因送货路线迁移而改变。

## 8. 新会话启动语

> 继续新版固定滑索送货上游迁移。我现在在公司/家里电脑，当前打开的是新版独立仓库。先读 AGENTS.md 和新版固定滑索送货接力记录，检查稳定分支 `codex/fixed-zipline-delivery`（远端保存点 `a8c21918`）与迁移分支 `codex/fixed-delivery-upstream-adapter`（保存点 `81254d00`）的本地和远端状态；不要先合并，先用已重建的候选 staging 复测武陵城林德路线，再按七终点与异常矩阵继续实机回归。

## 9. 2026-09-08 首次公司到家里交接（历史）

公司本轮完成环境准备、规范和计划同步，阶段停在 P0，尚未实现固定架序和连滑。用户确认武陵城布局未变，实机验证另择时间。

家里首次开始时：

1. 新建独立空目录，例如 `D:\Github project\MaaEnd V2`，以此作为新会话的项目根目录；不要打开旧 MaaEnd 目录继续旧分支。
2. 从个人仓库 `https://github.com/Hadrien-codeur/MaaEnd.git` 获取 `codex/fixed-zipline-delivery` 分支，按本记录第 5 节配置。该分支已经含上游基线及本次工作，不必先克隆上游主分支再覆盖。
3. 检查 GitHub 登录状态；家里若已登录同一账号可复用，否则在家里单独登录，不传输公司凭据。
4. 拉取后先读本记录、规范和已确认计划，再核对家里依赖、模型和测试程序。公司 `.cache`、`.venv`、`install` 和编译器不会随 Git 同步，不需要搬运整个公司目录。
5. 优先完成家里 C++ 工具链与本地编译，再按实机清单导入设施并验证上游路线。公司 C++ 下载是否结束不影响家里独立准备。
6. 家里收尾同样更新记录并推送本分支。下次回公司先检查本地状态、拉取家里的更新，再按变更重建程序，不能直接运行下班前的旧测试包。

本次交接提交只更新文档；用户自行启动的下载或安装进程未被本次交接操作暂停、取消或代为判定成功。

## 10. 2026-09-10 离开公司接力保存点

博士昨天回家未开发，本轮仍在公司新版目录；现在准备回家继续。已保存 [苏白易架序确认记录](./苏白易架序确认记录.md) 和 [地图预览 JSON](./苏白易架序.preview.json)，包含最终 15 架/14 跳及各架世界高度。记录中的 #0～#14 与编辑器一致；此前编号修正过程作废，以已确认表格为准。

本轮保存前逐一将 15 个点匹配到公司导入快照：地图、Level、模板和投影坐标均唯一匹配。此检查不代表供电、实际连线、上索或 E 接力已通过。地图工具已启动并显示导入架子；博士截图显示游戏连接未建立，仍无实机空跑结果。此前仅运行 --help 的检查不能称作 Agent 运行验证；2026-09-10 真正启动工具时已看到 AgentClient connected 和 Tasker initialized。

家里继续顺序：

1. 在独立新版仓库切到 codex/fixed-zipline-delivery，检查未提交改动，fetch 后仅快进同步。首次搭建按第 5 节；旧版仅供只读参考。
2. 先读本记录、开发规范、确认计划以及苏白易架序确认记录。称呼博士，避免重复要求提供已确认的坐标。
3. 独立核对 C++/Go/依赖并构建；公司二进制、.cache、.venv、node_modules 不随 Git 同步。MaaDeps 除目录存在外必须检查 maadeps.cmake、头文件与库完整。
4. 从 install/mxu.exe 的滑索导入任务登录森空岛，导入同一账号设施。原始快照和凭据保留在本机，不进入 Git。
5. 打开 MapNavigator，按架序记录恢复预览。预览 JSON 只有普通 NAVMESH 标记点，不具备固定架序执行能力，不能作为固定滑索试跑入口。
6. 继续 P0 实机基线、P1 逐跳与地面段证据，然后推进固定逐跳实现。连滑分段和 E 提示、失败策略尚未确认。

本次只保存文档和预览资料，无业务实现改动；资源全量检查沿用第 4 节历史结果，不声称本轮重新执行。此前尝试 pytest 失败是因为环境没有安装 pytest；MapNavigator 现有测试使用 unittest，后续按实际测试入口运行，不为此盲目加依赖。

完整设施快照、原始截图、账号配置、日志和程序不提交。家里不依赖公司浏览器、剪贴板或 localhost:8770 服务，重新启动本机工具即可。收尾检查文档格式、预览 JSON、Git 空白和远端 SHA 后完成同步。
